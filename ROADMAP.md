# ROADMAP

## Strategic objective (Production Order)
- Build a layered, capital-aware, bundle-aware decision engine.
- Optimize inventory turnover, capital efficiency, stockout risk, bundle composition efficiency, and assorti sustainability.
- Keep v1 deterministic and explainable; no black-box optimization.

## Product direction guardrails (accepted)
- Product direction is a modular vertical ERP / Supply Operating System for Wildberries, not a dashboard-only surface and not a planning-endpoint-only product.
- Current MVP-safe focus remains intentionally narrow:
  - supply planning engine
  - inventory truth
  - explainability and trust basics
  - from-WB parity
  - inbound and in-transit truth
  - receiving reconciliation
  - operational visibility basics
- Safe near-term work should stay in the `narrow_quality_pass_or_truth_hardening` lane:
  - runtime truth hardening
  - operator trust surfacing
  - small core cleanup
  - narrow availability-first hardening
- Not-now work remains out of immediate scope even if architecture should leave room for it:
  - broad dashboard build
  - deep simulation
  - full finance module
  - full cash engine
  - broad execution ERP
- Domain boundaries must stay explicit:
  - supplier procurement planning != WB internal replenishment
  - inventory truth != movement truth != recommendation output
  - finance facts != planning explainability
  - discrepancy events != stock balance adjustments
  - UI views != domain model
- Future modular domains must remain possible from current boundaries:
  - master_data
  - inventory_truth
  - supply_planning
  - inbound_tracking
  - receiving_reconciliation
  - wb_replenishment
  - leakage_control
  - sales_intelligence
  - finance
  - cash_planning
  - trust_and_exceptions

## Long-horizon modular roadmap order (directional, not immediate scope)
1. planning_core_and_truth_basics
2. operational_visibility
3. inbound_truth_and_receiving
4. wb_replenishment
5. reconciliation_and_leakage
6. sales_intelligence
7. finance
8. cash_planning
9. trust_operating_system

## Phase 0 - Monitoring reliability (completed)
- MonitoringScheduler integrated into app lifecycle.
- PostgreSQL advisory lock guard for single scheduler leader in multi-instance setup.
- Monitoring endpoints expanded with timeseries and risk-focus.

## Phase 0.5 - Engineering governance and quality gates (completed)
- CI workflow added for compile + automated tests on PR/push.
- Context synchronization CI guard added to enforce canonical docs updates.
- Contribution rules and Definition of Done documented.
- PR template and CODEOWNERS policy scaffold added.
- ADR process and release policy added.

## Phase 1 - Planning Core v1 contract (active)
- Health endpoint returns 200.
- Proposal endpoint accepts validated request body and returns structured proposal.
- Production-order direct/from-WB proposal APIs are stable and explainable.
- Keep API contract stable while replacing internals incrementally.

## Phase 2 - Production Order v1 Stable Alpha (active)

### 2.1 Layer 1 - Stock health metrics foundation (in progress)
- Per-SKU deterministic metrics: velocity (main/assorti), coverage, stock, in-flight, ETA, margin proxy, capital proxy, stockout/overstock risk.
- Assorti classification now uses explicit `bundle_type.is_assorti` as primary source with deterministic admin/global fallback mapping (`admin_defaults` -> `global_default`) and traceable meta output (no runtime keyword parsing).
- Machine-readable metrics and contract checks are exposed in explanation meta (risk bounds, non-negative invariants, unique SKU pairs).
- Decision-quality case-study regression now includes full helper-chain `L1 -> L5` deterministic coverage to lock computed Layer 1 metric stability together with downstream allocation/shaping/scenario/intervention behavior.

### 2.2 Layer 2 - Allocation comparison engine (in progress)
- Deterministic composite-objective comparison is the primary decision gate (`objective_score_if_main_until_eta` vs `objective_score_if_assorti_until_eta`, `decision_gate=composite_objective_until_eta`) computed from traceable economics inputs (realized price, commission, production cost, logistics cost).
- GMROI proxy is computed for diagnostics/audit; deterministic tie-break is `hold`.
- Layer 2 now emits explicit decision-quality diagnostics (near-tie/tie counts, decision reason distribution, avg profit/GMROI gaps, capital-locked aggregates) in explainability meta.
- Layer 2 default presentation now uses canonical composite-objective naming (`method=time_window_composite_objective_with_gmroi_diagnostics`, `decision_gate=composite_objective_until_eta`) while keeping explicit legacy aliases (`legacy_method=time_window_profit_proxy_with_gmroi_diagnostics`, `legacy_decision_gate=profit_until_eta`) to support transition without facade drift.
- Layer 2 now emits explicit legacy-alias deprecation-plan diagnostics (`deprecated_after`, legacy gate aliases, canonical field replacement map) in decision-quality and explainability meta/alpha-proxy blocks while keeping aliases non-breaking during the transition window.
- Layer 2 decision flags (`allocation_decision`, `tie_break_applied`, `near_tie`) are stabilized against floating-point boundary noise by using normalized 4-decimal profit/GMROI diagnostics that are also emitted in explainability payloads.
- Layer 2 helper interfaces now require explicit economics inputs (`margin_main_per_unit`, `margin_assorti_per_unit`, `unit_capital_per_unit`) to prevent accidental fallback to proxy constants after refactors.
- Explicit decision (`main` / `assorti` / `hold`) per SKU.
- Layer 2 contract summary is now exposed (version/checks/status; summary-vs-decisions consistency, allocation-vs-composite-objective-gate consistency with legacy alias compatibility, tie-break invariants, decision-reason mapping including objective-score reason field consistency, tie/near-tie objective-gap consistency with legacy profit-gap aliases, profit/GMROI gap consistency, objective-score-gap consistency, capital metric sanity) and projected in compact explainability mode.
- Layer 2 contract now explicitly verifies objective-component decomposition formula consistency (`objective_score = expected_gross_profit - capital_cost_penalty - stockout_penalty - overstock_penalty`) to block silent fallback or malformed objective payloads.
- Capital-limited line selection evidence is now regression-locked at helper/API level: objective-per-capital ranking can prioritize lower gross-profit lines when penalties make them economically dominant, budget allocation follows this ranking (no silent profit-only fallback), and proposal meta exposes deterministic budget-limited ranking/cutoff behavior.
- Shared-capital tie-break now prioritizes availability risk deterministically when objective metrics are equal (`stockout_risk` higher first, then `overstock_risk` lower) to align constrained allocation with out-of-stock protection policy.
- Capital-constraint meta now includes runtime contract summary (`version/checks/status`) validating budget accounting, line-count invariants, ranking sort/uniqueness, risk-priority score consistency, and cutoff-line consistency to guard against silent ranking/accounting drift.
- No hard-coded "critical SKU" classifier as primary allocator.
- Economic Alpha economics precedence is now wired and traceable end-to-end (`request -> admin_defaults -> global_default -> code_default_constants`) with regression coverage for each source tier.

### 2.3 Layer 3 - Purchase recommendation (in progress)
- Allocation-driven purchase shaping now includes deterministic risk-weighted calibration on top of Layer 2 decisions.
- Regression now explicitly locks Layer 3 semantics as quantity-shaping only: changing Layer 3 calibration overrides changes shaped quantity/reorder units, while Layer 2 allocation summary/decisions remain unchanged.
- Layer 3 calibration coefficients now support deterministic precedence (`request -> admin_defaults -> global_default -> code_default_constants`) with source tracing in explainability meta.
- Layer 3 contract summary is now exposed (version/checks/status; qty delta invariants, decision/risk partition consistency, calibration method/bounds, factor summary sanity) and projected in compact explainability mode.
- Stabilize Layer 3 calibration coefficients/bounds and lock regression coverage.
- Preserve deterministic math and full explainability.

### 2.4 Layer 4 - Scenario output (in progress)
- Deterministic scenarios are wired in explanation meta: Conservative / Balanced / Aggressive.
- Per-scenario outputs now include money fields (`expected_revenue`, `expected_gross_profit`, `expected_margin_percent`, `expected_turnover_days`) plus risk proxies (`stockout_probability_proxy`, `stockout_risk_proxy`, `overstock_risk_proxy`) and assorti sustainability proxy/impact.
- Capital-gap transparency is now emitted (`available_capital`, `required_capital`, `deficit_or_surplus`) without auto-policy enforcement.
- Layer 4 contract now includes scenario-delta runtime checks (delta field presence + balanced-baseline consistency across capital/revenue/profit/objective, including gross-profit delta alias consistency) to prevent silent explainability drift.
- Finalize calibration rules and intervention handoff contract.

### 2.7 Economic Alpha calibration (active, highest priority)
- Replace proxy-only economics with formula-based, source-traceable economics inputs.
- Keep deterministic behavior and API contract stability while migrating.
- Scope guard for this block is strict: no ML, no solver optimization, no multi-warehouse rollout, no new non-economics entities.
- from-WB adapter now feeds observed realized prices from WB revenue window into runtime economics source `from_wb_observed_window` (request still has top precedence), with deterministic anomaly filtering (`30%` max deviation) and diagnostics projected in explainability meta.
- Execution order inside this block:
  1. economics input traceability (`request -> admin_defaults -> global_default -> code_default_constants`),
  2. Layer 2 composite-objective gate hardening with legacy alias compatibility,
  3. Layer 4 full money outputs + capital-gap diagnostics,
  4. targeted decomposition of layer helpers into modules without facade behavior drift.
- Release evidence now includes deterministic end-to-end allocation flip under identical units-until-ETA where only realized price inputs are swapped (`main -> assorti`), executed in the integration gate test set used by `verify`/`verify-live`.
- Trusted-economics diagnostics are now a release-block requirement: expose per-key-field economics source, code-default dominance ratio, and trust level (`trusted|partial|untrusted`) with explicit warning block in full/compact explainability.
- Capital governance is now explicit and backward-compatible for proposal execution: default mode remains `strict` (unresolved `available_capital` from `request|admin_defaults|global_default` returns deterministic `422` with actionable detail and explicit `capital_constraint_status=missing_available_capital_strict`), while request-level opt-in `capital_governance_mode=safe_default` applies a safe zero-capital fallback with `HIGH` severity warning and explainability/meta tracing instead of unconstrained recommendation flow.
- Value-proof sequencing is now explicit: lock trusted economics + capital safety + multi-regime e2e evidence first; defer large modular refactor work until these proofs are stable.
- Narrow R5 work may now proceed only as facade-preserving extraction:
  - First safe slice is the shared economics helper cluster (`planning_production_order_economics.py`) with public behavior unchanged and existing service exports preserved from `planning_production_order.py`.
  - Second safe slice for narrow R5 is compact explainability projection (`planning_production_order_explainability.py`): compact/full response behavior stays regression-locked while `planning_production_order.py` preserves compatibility imports and facade behavior.
  - Third safe slice for narrow R5 is assorti classification support (`planning_production_order_assorti.py`): assorti mapping parse/load helpers and source constants move out, while `planning_production_order.py` keeps compatibility imports and deterministic Layer 1 explainability behavior unchanged.
  - Fourth safe slice for narrow R5 is layer-proxy settings resolution (`planning_production_order_layer_proxy.py`): default coefficients, threshold/source precedence, and threshold-clamp behavior move out, while `planning_production_order.py` keeps compatibility imports and response semantics unchanged.
  - Fifth safe slice for narrow R5 is layer-proxy invalid-value runtime warning: if persisted Layer 3/5 proxy values fall outside the valid `[0,1]` interval and get ignored at runtime, full/compact explainability must emit a dedicated `MEDIUM` severity warning (`layer_proxy_invalid_values_ignored_at_runtime`) instead of silently falling through to lower-precedence or code-default values.
  - Sixth safe slice for narrow R5 is constraint-based supply-alignment helper ownership (`planning_production_order_constraints.py`): resource-allocation contract/build helpers and shared-pantone-pool snapshot logic move out, while `planning_production_order.py` keeps facade exports and runtime response semantics unchanged.
  - Seventh safe slice for narrow R5 is capital constraint and Layer 4 helper ownership (`planning_production_order_capital.py`): objective-per-capital ranking, budget application, capital gap/contract summaries, and Layer 4 scenario/aggregate helpers move out, while `planning_production_order.py` keeps facade exports and runtime response semantics unchanged.
  - Eighth safe slice for narrow R5 is Layer 1/2 decision ownership (`planning_production_order_decision.py`): stock-health metrics, Layer 1/2 contract summaries, Layer 2 allocation decisions, decision-quality diagnostics, and legacy-alias deprecation-plan helpers move out, while `planning_production_order.py` keeps facade exports and runtime response semantics unchanged.
  - Ninth safe slice for narrow R5 is Layer 3 purchase-shaping ownership (`planning_production_order_layer3.py`): decision-based purchase factors, calibration bounds/weights, Layer 3 shaping, and Layer 3 contract summary move out, while `planning_production_order.py` keeps facade exports and runtime response semantics unchanged.
  - Tenth safe slice for narrow R5 is Layer 5 intervention ownership (`planning_production_order_layer5.py`): intervention signal derivation, Layer 5 contract summary, and threshold-clamp operator warning move out, while `planning_production_order.py` keeps facade exports and runtime response semantics unchanged.
  - Eleventh safe slice for narrow R5 is operator-contract/detail ownership (`planning_production_order_operator_contracts.py`): machine-readable from-WB freshness failure details, direct prerequisite failure details, article/capital governance details, and operator warnings move out, while `planning_production_order.py` keeps facade exports, governance constants, and runtime response semantics unchanged.
  - Twelfth safe slice for narrow R5 is from-WB adapter/calibration ownership (`planning_production_order_from_wb.py`): WB mapping detail builders, mapped-bundle resolution, WB stock/sales loaders, and observed price/commission calibration helpers move out, while `planning_production_order.py` keeps facade exports, monkeypatch-compatible `httpx` surface, and final response/orchestration semantics unchanged.
  - Thirteenth safe slice for narrow R5 is supply-policy ownership (`planning_production_order_supply_policy.py`): in-flight stage weighting, effective in-flight quantity estimation, and economic-buffer policy helpers move out, while `planning_production_order.py` keeps facade exports and final recommendation/orchestration semantics unchanged.
  - Fourteenth safe slice for narrow R5 is settings/defaults ownership (`planning_production_order_settings.py`): effective settings resolution, admin size-weight loading, and admin in-flight defaults loading move out, while `planning_production_order.py` keeps facade exports, the `_EffectiveSettings` contract surface, and final recommendation/orchestration semantics unchanged.
  - Fifteenth safe slice for narrow R5 is elastic binding scope ownership (`planning_production_order_elastic.py`): active elastic-type applicability and scoped line-key resolution move out, while `planning_production_order.py` keeps facade exports and final recommendation/orchestration semantics unchanged.
  - Sixteenth safe slice for narrow R5 is competition-aware raw stock ownership (`planning_production_order_raw_stock.py`): competition-aware raw bundle stock estimation moves out, while `planning_production_order.py` keeps facade exports and final recommendation/orchestration semantics unchanged.
  - Seventeenth safe slice for narrow R5 is capital helper facade cleanup (`planning_production_order_capital.py`): `_bounded_unit_float` and `_compute_objective_components` are now owned only by the capital module, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Eighteenth safe slice for narrow R5 is allocation/uplift math ownership (`planning_production_order_math.py`): integer ceiling, weight normalization, unit allocation, and per-color uplift helpers move out, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Nineteenth safe slice for narrow R5 is from-WB freshness ownership cleanup (`planning_production_order_freshness.py`): freshness snapshot and DB-backed threshold resolution now live only in the freshness module, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Twentieth safe slice for narrow R5 is Layer 2 legacy-alias deprecation-plan facade cleanup (`planning_production_order_decision.py`): `_build_layer2_legacy_alias_deprecation_plan` is now owned only by the decision module, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Twenty-first safe slice for narrow R5 is Layer 1 stock-health helper ownership cleanup (`planning_production_order_decision.py`): `_build_layer1_stock_health_metrics` and `_build_layer1_contract_summary` are now owned only by the decision module, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Twenty-second safe slice for narrow R5 is Layer 2 decision-helper ownership cleanup (`planning_production_order_decision.py`): `_build_layer2_allocation_decisions`, `_build_layer2_contract_summary`, and `_build_layer2_decision_quality_summary` are now owned only by the decision module, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Twenty-third safe slice for narrow R5 is Layer 3 purchase-shaping ownership cleanup (`planning_production_order_layer3.py`): `_apply_layer3_purchase_shaping` and `_build_layer3_contract_summary` are now owned only by the Layer 3 module, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Twenty-fourth safe slice for narrow R5 is Layer 4/capital ownership cleanup (`planning_production_order_capital.py`): `_build_layer4_scenarios`, `_build_line_objective_capital_rankings`, `_apply_capital_constraint_to_candidate_lines`, `_build_capital_constraint_contract_summary`, `_build_capital_gap_summary`, `_build_layer4_contract_summary`, and `_build_layer4_aggregate_deltas` are now owned only by the capital module, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Twenty-fifth safe slice for narrow R5 is Layer 5 intervention ownership cleanup (`planning_production_order_layer5.py`): `_build_layer5_intervention_signals` and `_build_layer5_contract_summary` are now owned only by the Layer 5 module, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Twenty-sixth safe slice for narrow R5 is constraints constant ownership cleanup (`planning_production_order_constraints.py`): `SHARED_COLOR_POOL_SOURCE`, `SHARED_COLOR_POOL_DEFAULT_OBSERVATION_WINDOW_DAYS`, and `RESOURCE_ALLOCATION_CONTRACT_VERSION` are now owned only by the constraints module, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Twenty-seventh safe slice for narrow R5 is elastic uplift ownership extraction (`planning_production_order_elastic.py`): `_apply_elastic_min_batch_uplift` now owns binding-scope resolution, elastic target selection, uplift allocation, and emitted elastic constraint payloads, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Twenty-eighth safe slice for narrow R5 is shared color pool fabric uplift ownership extraction (`planning_production_order_constraints.py`): `_apply_shared_color_pool_fabric_min_batches` now owns shared pantone pool snapshot consumption, fabric min-batch uplift allocation, and emitted fabric constraint payloads, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Twenty-ninth safe slice for narrow R5 is explainability warning aggregation ownership extraction (`planning_production_order_explainability.py`): `_build_explanation_warnings` now owns warning list aggregation for economics trust passthrough, invalid layer-proxy diagnostics, Layer 5 threshold clamp diagnostics, and shortage-wait/capital-blocked diagnostics, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Thirtieth safe slice for narrow R5 is economics trust and capital governance ownership extraction (`planning_production_order_economics.py`): `_resolve_economic_trust_and_capital_governance` now owns economics-trust diagnostics, capital governance mode resolution, safe-default zero-capital fallback application, and strict missing-capital detail selection, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Thirty-first safe slice for narrow R5 is alpha-proxy economics meta ownership extraction (`planning_production_order_explainability.py`): `_build_alpha_proxy_economics_meta` now owns `alpha_proxy_economics` packaging for economics source/input tracing, Layer 2/3/4/5 calibration diagnostics, and capital-governance/economics-trust surfacing, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Thirty-second safe slice for narrow R5 is explanation steps ownership extraction (`planning_production_order_explainability.py`): `_build_explanation_steps` now owns full explainability step-string packaging for demand/scope/source/layer/capital/elastic diagnostics, while `planning_production_order.py` keeps facade exports, from-WB adapter prepend behavior, and runtime semantics unchanged.
  - Thirty-third safe slice for narrow R5 is explanation meta ownership extraction (`planning_production_order_explainability.py`): `_build_explanation_meta` now owns full explainability meta packaging for source/scope/layer/capital/resource/elastic diagnostics, while `planning_production_order.py` keeps facade exports, from-WB post-processing, and runtime semantics unchanged.
  - Thirty-fourth safe slice for narrow R5 is from-WB explainability ownership extraction (`planning_production_order_explainability.py`): `_apply_from_wb_explainability` now owns `from_wb` meta packaging and `WB ingestion adapter` step prepend behavior, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Thirty-fifth safe slice for narrow R5 is from-WB runtime economic overrides ownership extraction (`planning_production_order_from_wb.py`): `_build_from_wb_runtime_economic_overrides` now owns observed price/commission normalization plus runtime economics override and per-field source-override packaging, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Thirty-sixth safe slice for narrow R5 is from-WB explainability input packaging ownership extraction (`planning_production_order_explainability.py`): `_build_from_wb_explainability_inputs` now owns as-of source resolution, sales-window derivation, bundle snapshot packaging, and text-formatted freshness fields for from-WB explainability application, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Thirty-seventh safe slice for narrow R5 is from-WB proposal request ownership extraction (`planning_production_order_from_wb.py`): `_build_from_wb_proposal_request` now owns delegated `ProductionOrderProposalRequest` assembly, including bundle demand and bundle stock packaging for the from-WB flow, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Thirty-eighth safe slice for narrow R5 is from-WB strict freshness gate ownership extraction (`planning_production_order_freshness.py`): `_raise_from_wb_strict_freshness_failure_if_needed` now owns strict freshness rejection and structured `400` raising for stale/no-data from-WB inputs via injected detail-builder callback, while `planning_production_order.py` keeps facade exports and runtime semantics unchanged.
  - Thirty-ninth safe slice for narrow R5 is from-WB preflight preparation ownership extraction (`planning_production_order_from_wb.py`): `_build_from_wb_preflight_context` now owns bundle mapping resolution, sales/stock snapshot loading, observed calibration loading, freshness threshold/snapshot preparation, strict freshness enforcement, delegated request assembly, and runtime override packaging for the from-WB flow, while `planning_production_order.py` keeps façade compatibility surfaces (`httpx`, `EXPLAINABILITY_MODE_FULL`), response assembly, and runtime semantics unchanged.
  - Fortieth safe slice for narrow R5 is from-WB explainability finalization ownership extraction (`planning_production_order_explainability.py`): `_finalize_from_wb_explainability` now owns from-WB explainability input packaging, `from_wb` meta/step augmentation, and requested explainability-mode finalization for the from-WB response path, while `planning_production_order.py` keeps façade compatibility surfaces (`httpx`, `EXPLAINABILITY_MODE_FULL`), core proposal orchestration, and runtime semantics unchanged.
  - Forty-first safe slice for narrow R5 is typed from-WB preflight context ownership extraction (`planning_production_order_from_wb.py`): `_FromWbPreflightContext` now defines the authoritative owner-side contract for `_build_from_wb_preflight_context`, replacing the loose dict payload with a frozen typed dataclass while `planning_production_order.py` keeps façade compatibility surfaces (`httpx`, `EXPLAINABILITY_MODE_FULL`), consumes the context via attribute access, and preserves runtime semantics unchanged.
  - Forty-second safe slice for narrow R5 is full from-WB façade orchestration ownership extraction (`planning_production_order_from_wb.py`): `_build_production_order_proposal_from_wb_response` now owns article existence validation, preflight orchestration, delegated core proposal invocation, and response-side from-WB explainability finalization via injected callbacks, while `planning_production_order.py` keeps façade compatibility surfaces (`httpx`, `EXPLAINABILITY_MODE_FULL`) and a thin public wrapper unchanged.
  - Forty-third safe slice for narrow R5 is from-WB mapping operator-contract ownership extraction (`planning_production_order_operator_contracts.py`): `_build_from_wb_no_mapping_detail` and `_build_from_wb_missing_requested_bundle_type_detail` now live with the rest of the machine-readable operator-contract builders, while `planning_production_order_from_wb.py` consumes/re-exports them and `planning_production_order.py` preserves façade compatibility surfaces (`httpx`, `EXPLAINABILITY_MODE_FULL`) and helper names unchanged.
  - Forty-fourth safe slice for narrow R5 is shared article existence helper ownership extraction (`planning_production_order_article.py`): `_build_article_not_found_detail` and `_require_article` now live in a dedicated shared owner module used by `planning_production_order.py`, `planning_production_order_from_wb.py`, and `planning_production_order_admin.py`, while `planning_production_order_operator_contracts.py` re-exports the detail builder and `planning_production_order.py` preserves façade compatibility surfaces (`httpx`, `EXPLAINABILITY_MODE_FULL`) plus helper names unchanged.
  - Forty-fifth safe slice for narrow R5 is admin settings operator-contract ownership extraction (`planning_production_order_operator_contracts.py`): `_build_admin_settings_field_metadata` and `_build_admin_settings_validation_detail` now live with the rest of the machine-readable operator-contract builders, while `planning_production_order_admin.py` consumes/re-exports them and runtime validation semantics remain unchanged.
  - Forty-sixth safe slice for narrow R5 is production-order input preparation ownership extraction (`planning_production_order_inputs.py`): `_PreparedProductionOrderInputs` and `_prepare_production_order_inputs` now own bundle recipe/SKU scope preparation, size-weight resolution, stock aggregation, in-flight effective supply normalization, WB bundle-stock fallback, and bundle demand/share packaging, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Forty-seventh safe slice for narrow R5 is production-order line requirements ownership extraction (`planning_production_order_line_requirements.py`): `_LineRequirementsPlan` and `_build_line_requirements_plan` now own deficit-to-line allocation preparation, including color probability derivation, per-color size-weighted requirement allocation, and residual `line_qty` computation against current stock, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Forty-eighth safe slice for narrow R5 is production-order constraint application ownership extraction (`planning_production_order_constraint_application.py`): `_ConstraintApplicationResult` and `_apply_production_order_constraints` now own pantone/color override preparation plus fabric/shared-color and elastic constraint application orchestration, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Forty-ninth safe slice for narrow R5 is production-order candidate line assembly ownership extraction (`planning_production_order_candidate_lines.py`): `_build_candidate_lines` now owns recommendation-line assembly from final `line_qty` plus per-line Layer 3/Layer 2 decision tagging in `source_reason`, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Fiftieth safe slice for narrow R5 is production-order capital application ownership extraction (`planning_production_order_capital_application.py`): `_CapitalApplicationResult` and `_apply_production_order_capital_constraints` now own capital ranking orchestration, budget-constrained candidate-line application, capital contract packaging, and `candidate_total_units` derivation, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Fifty-first safe slice for narrow R5 is production-order Layer 4 application ownership extraction (`planning_production_order_layer4_application.py`): `_Layer4ApplicationResult` and `_apply_production_order_layer4_analysis` now own expected-horizon-sales derivation plus Layer 4 scenario building, capital-gap packaging, contract summary packaging, and aggregate-delta packaging, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Fifty-second safe slice for narrow R5 is production-order Layer 5 application ownership extraction (`planning_production_order_layer5_application.py`): `_Layer5ApplicationResult` and `_apply_production_order_layer5_analysis` now own Layer 5 intervention-signal orchestration, contract packaging, and `layer5_intervention_meta` packaging, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Fifty-third safe slice for narrow R5 is production-order scope/recommendation ownership extraction (`planning_production_order_scope_recommendation.py`): `_ScopeRecommendationResult` and `_apply_production_order_scope_and_recommendation` now own physical-scope/arrival-projection orchestration plus recommendation/alternatives packaging, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Fifty-fourth safe slice for narrow R5 is production-order explanation-warnings ownership extraction (`planning_production_order_explanation_warning_application.py`): `_ExplanationWarningApplicationResult` and `_apply_production_order_explanation_warnings` now own explanation-warning orchestration over runtime layer-proxy, arrival-projection, and capital-constraint inputs, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Fifty-fifth safe slice for narrow R5 is production-order alpha-proxy economics ownership extraction (`planning_production_order_alpha_proxy_application.py`): `_AlphaProxyApplicationResult` and `_apply_production_order_alpha_proxy_economics` now own `layer4_scenario_factor_items` derivation plus alpha-proxy economics packaging for explainability/meta emission, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Fifty-sixth safe slice for narrow R5 is production-order explanation assembly ownership extraction (`planning_production_order_explanation_application.py`): `_ExplanationApplicationResult` and `_apply_production_order_explanation` now own `ProductionOrderExplanationBlock` assembly, including summary construction plus `_build_explanation_steps` and `_build_explanation_meta` orchestration, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Fifty-seventh safe slice for narrow R5 is production-order explainability-mode application ownership extraction (`planning_production_order_explainability_mode_application.py`): `_ExplainabilityModeApplicationResult` and `_apply_production_order_explainability_mode` now own final explainability-mode application over `ProductionOrderExplanationBlock`, while `planning_production_order.py` keeps façade compatibility helper names, skipped-path behavior, and runtime semantics unchanged.
  - Fifty-eighth safe slice for narrow R5 is production-order response assembly ownership extraction (`planning_production_order_response_application.py`): `_ResponseApplicationResult` and `_apply_production_order_response` now own final `ProductionOrderProposalResponse` assembly for both `skipped` and `ok` production-order paths, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Fifty-ninth safe slice for narrow R5 is production-order skipped-path ownership extraction (`planning_production_order_skip_application.py`): `_SkipApplicationResult` and `_apply_production_order_skip` now own the `include_in_planning=false` short-circuit path, including skipped explanation construction, explainability-mode application, and skipped response assembly orchestration, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Sixtieth safe slice for narrow R5 is production-order economic-governance application ownership extraction (`planning_production_order_economic_governance_application.py`): `_EconomicGovernanceApplicationResult` and `_apply_production_order_economic_governance` now own economic-trust/capital-governance application orchestration, including strict missing-capital `422` short-circuit handling, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Sixty-first safe slice for narrow R5 is production-order settings-resolution application ownership extraction (`planning_production_order_settings_resolution_application.py`): `_SettingsResolutionApplicationResult` and `_apply_production_order_settings_resolution` now own pre-pipeline effective settings, layer-proxy settings, and economic-settings resolution orchestration, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Sixty-second safe slice for narrow R5 is production-order settings-loading application ownership extraction (`planning_production_order_settings_loading_application.py`): `_SettingsLoadingApplicationResult` and `_apply_production_order_settings_loading` now own article/planning/global settings DB loading orchestration for the direct production-order path, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Sixty-third safe slice for narrow R5 is production-order resource-allocation application ownership extraction (`planning_production_order_resource_allocation_application.py`): `_ResourceAllocationApplicationResult` and `_apply_production_order_resource_allocation` now own resource-allocation orchestration plus `competition_raw_*` and `available_bundles_for_cover` packaging, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Sixty-fourth safe slice for narrow R5 is production-order assorti-classification application ownership extraction (`planning_production_order_assorti_application.py`): `_AssortiApplicationResult` and `_apply_production_order_assorti_classification` now own admin/global assorti mapping parse, bundle-type assorti classification loading, assorti/main counts, and classification-source breakdown packaging, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Sixty-fifth safe slice for narrow R5 is production-order horizon application ownership extraction (`planning_production_order_horizon_application.py`): `_HorizonApplicationResult` and `_apply_production_order_horizon` now own economic-buffer calculation plus target-horizon, required-bundles, and bundle-deficit packaging, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Sixty-sixth safe slice for narrow R5 is production-order risk application ownership extraction (`planning_production_order_risk_application.py`): `_RiskApplicationResult` and `_apply_production_order_risk_level` now own `days_of_cover_estimate` and `risk_level` derivation for the direct production-order path, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Sixty-seventh safe slice for narrow R5 is production-order layer1-summary application ownership extraction (`planning_production_order_layer1_summary_application.py`): `_Layer1SummaryApplicationResult` and `_apply_production_order_layer1_summary` now own average coverage derivation, high-stockout-risk counting, and Layer 1 contract packaging, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Sixty-eighth safe slice for narrow R5 is production-order layer2-summary application ownership extraction (`planning_production_order_layer2_summary_application.py`): `_Layer2SummaryApplicationResult` and `_apply_production_order_layer2_summary` now own Layer 2 contract packaging and decision-quality packaging after allocation-decision derivation, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Sixty-ninth safe slice for narrow R5 is production-order layer2-allocation application ownership extraction (`planning_production_order_layer2_allocation_application.py`): `_Layer2AllocationApplicationResult` and `_apply_production_order_layer2_allocation` now own Layer 2 allocation-decision derivation and summary packaging, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Seventieth safe slice for narrow R5 is production-order line-requirements application ownership extraction (`planning_production_order_line_requirements_application.py`): `_LineRequirementsApplicationResult` and `_apply_production_order_line_requirements` now own line-requirements plan orchestration plus `color_probability`, `line_required`, and `line_qty` packaging, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Seventy-first safe slice for narrow R5 is production-order layer3 application ownership extraction (`planning_production_order_layer3_application.py`): `_Layer3ApplicationResult` and `_apply_production_order_layer3` now own Layer 3 purchase-shaping orchestration plus `layer3_decision_by_line`, `layer3_purchase_shaping`, and `layer3_contract` packaging, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Seventy-second safe slice for narrow R5 is production-order candidate-lines application ownership extraction (`planning_production_order_candidate_lines_application.py`): `_CandidateLinesApplicationResult` and `_apply_production_order_candidate_lines` now own candidate-line assembly orchestration for the direct production-order path, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Seventy-third safe slice for narrow R5 is production-order capital-unpack application ownership extraction (`planning_production_order_capital_unpack_application.py`): `_CapitalUnpackApplicationResult` and `_apply_production_order_capital_unpack` now own post-capital result projection for the direct production-order path, including `candidate_lines`, `capital_rankings`, `capital_constraint_summary`, `capital_constraint_contract`, and `candidate_total_units`, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Seventy-fourth safe slice for narrow R5 is production-order layer4-unpack application ownership extraction (`planning_production_order_layer4_unpack_application.py`): `_Layer4UnpackApplicationResult` and `_apply_production_order_layer4_unpack` now own post-Layer-4 result projection for the direct production-order path, including `expected_horizon_sales`, `layer4_scenarios`, `capital_gap_summary`, `layer4_contract`, and `layer4_aggregate_deltas`, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Seventy-fifth safe slice for narrow R5 is production-order layer5-unpack application ownership extraction (`planning_production_order_layer5_unpack_application.py`): `_Layer5UnpackApplicationResult` and `_apply_production_order_layer5_unpack` now own post-Layer-5 result projection for the direct production-order path, including `layer5_intervention`, `layer5_contract`, and `layer5_intervention_meta`, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Seventy-sixth safe slice for narrow R5 is production-order scope-recommendation-unpack application ownership extraction (`planning_production_order_scope_recommendation_unpack_application.py`): `_ScopeRecommendationUnpackApplicationResult` and `_apply_production_order_scope_recommendation_unpack` now own post-scope-recommendation result projection for the direct production-order path, including `physical_scope`, `arrival_projection`, `action`, `recommendation`, and `alternatives`, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Seventy-seventh safe slice for narrow R5 is production-order explanation-warning-unpack application ownership extraction (`planning_production_order_explanation_warning_unpack_application.py`): `_ExplanationWarningUnpackApplicationResult` and `_apply_production_order_explanation_warning_unpack` now own post-explanation-warning result projection for the direct production-order path, including `explanation_warnings`, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
  - Seventy-eighth safe slice for narrow R5 is production-order alpha-proxy-unpack application ownership extraction (`planning_production_order_alpha_proxy_unpack_application.py`): `_AlphaProxyUnpackApplicationResult` and `_apply_production_order_alpha_proxy_unpack` now own post-alpha-proxy result projection for the direct production-order path, including `layer4_scenario_factor_items` and `alpha_proxy_economics`, while `planning_production_order.py` keeps façade compatibility helper names and runtime semantics unchanged.
 
 - Constraint-based supply alignment has started on the existing production-order path: Phase 1 resource allocation core is now implemented per article via explicit shared-resource reservation (`color x size`) across bundle strategies, deterministic no-double-use contract checks, and emitted allocation-consumption diagnostics in `constraints_applied` + full/compact explainability.
- Constraint-based supply alignment now includes Phase 2 cross-article color linking on the existing production-order facade: sibling WB article demand is aggregated into a shared pantone pool proxy that influences fabric minimum batching decisions, and the applied shared-pool evidence is emitted in `constraints_applied.fabric_min_batches` plus full/compact explainability metadata.
- Production-order API contract now explicitly exposes physical stock scope and arrival-horizon projection at the top level (`physical_scope`, `arrival_projection`) and mirrors both blocks in explainability metadata; compact mode preserves these blocks as first-class diagnostics rather than dropping them into untyped tails.
- Arrival-horizon projection is now part of the live decision guardrail on the production-order facade: `safe_cover_until_arrival` may deterministically force `wait`, while `shortage_before_arrival` remains an order-driving/escalation signal without introducing deep simulation.
- Availability-first trust surfacing is now explicitly hardened for capital-blocked shortages: if `arrival_projection.status=shortage_before_arrival` but capital constraint trims feasible order quantity to zero and the facade remains `wait`, full/compact explainability must emit a `HIGH` severity blocked-shortage warning instead of leaving that wait path unqualified.
- Layer 5 threshold-order runtime truth is now explicitly hardened as well: if `layer5_accelerate_production_risk_threshold` is configured below `layer5_unavoidable_stockout_risk_threshold` and gets clamped upward at runtime, full/compact explainability must emit a dedicated `MEDIUM` severity warning (`layer5_accelerate_threshold_clamped_to_unavoidable`) instead of relying only on proxy metadata (`layer5_threshold_order_adjusted` / `|clamped_to_unavoidable`).
- Legacy planning paths are now explicitly marked low-fidelity/deprecated at the API surface: `/api/v1/planning/core/proposal` remains a stub with successor guidance, and `/api/v1/planning/order-proposal` remains legacy live logic with deprecation/fidelity headers instead of silent parity assumptions.
- Direct production-order prerequisite failures should stay operator-facing and machine-readable: missing `bundle_recipe` coverage and missing SKU scope for recipe colors now return structured `400` details (`code/message/affected ids/next_steps`) and must not regress back to plain string errors.
- Production-order settings admin validation should stay machine-readable as well: invalid size ids, elastic binding scope mismatches, assorti bundle type ids, and in-flight color/size scope errors now return structured `400` details (`code/field/affected ids/next_steps`) instead of plain strings.
- WB onboarding/live-sync account resolution should stay machine-readable too: missing active account, unknown `account_id`, and empty `api_token` now return structured `400` details (`code/account_id?/next_steps`) instead of plain strings before any external WB call is attempted.
- WB transport/response failures should also stay machine-readable: request/network failures, rate limits, unauthorized tokens, invalid JSON, and invalid rows-format payloads now return structured WB error details with deterministic `code`, `next_steps`, and upstream metadata where available.
- WB object-response helpers should stay machine-readable as well: `_wb_get_json_object` now returns structured `wb_api_invalid_object_format` detail with deterministic `next_steps` instead of a raw string when an object endpoint responds with a non-object payload.
- WB replenishment request validation should stay machine-readable too: invalid date ordering (`wb_arrival_date < target_date`) on `POST /api/v1/wb/manager/proposal` now returns a structured validation detail with both dates, invalid field, and deterministic `next_steps`.
- WB shipment creation validation should stay aligned too: invalid date ordering (`wb_arrival_date < target_date`) on `POST /api/v1/wb/manager/shipment/from-proposal` now returns the same structured validation detail with both dates, invalid field, and deterministic `next_steps`.
- WB shipment resource misses should stay machine-readable too: read/edit shipment surfaces now return structured `wb_shipment_not_found` / `wb_shipment_item_not_found` details with resource IDs and deterministic `next_steps` instead of raw strings.
- WB shipment headers sorting should stay machine-readable too: invalid `sort_by` / `sort_dir` now return structured validation details, and the header-list path no longer crashes due to `status` query shadowing.
- WB shipment edit restrictions should stay machine-readable too: final shipment locks, non-draft item locks, and `final_qty > nsk_stock_available` now return structured validation details with shipment/item IDs and deterministic `next_steps` instead of raw strings.
  - WB shipment status mutations should stay machine-readable too: unsupported status values and invalid status transitions now return structured validation details with shipment id, field context, allowed values/targets, and deterministic `next_steps` instead of raw strings.
  - Planning monitoring timeseries validation should stay machine-readable too: missing `metrics` query parameter on `GET /api/v1/planning/monitoring/timeseries` now returns a structured `400` detail with field context and deterministic `next_steps` instead of a raw string.
  - Planning monitoring alert-rule admin should stay machine-readable too: missing alert rules on `PATCH` / `DELETE /api/v1/planning/monitoring/alert-rules/{rule_id}` now return structured `alert_rule_not_found` details with `rule_id` and deterministic `next_steps` instead of a raw string.
  - Purchase-order status mutations should stay machine-readable too: unsupported status values and invalid status transitions now return structured validation details with order id, field context, allowed values/targets, and deterministic `next_steps` instead of raw strings; cancelled orders continue to advertise self-transition semantics in that contract.
  - Purchase-order read/item-edit guardrails should stay machine-readable too: missing purchase orders, missing purchase-order items, and non-draft item-edit locks now return structured details with order/item ids, status context where relevant, and deterministic `next_steps` instead of raw strings.
  - Warehouse admin CRUD should stay machine-readable too: missing warehouses and duplicate warehouse codes now return structured `warehouse_not_found` / `warehouse_code_already_exists` details with ids or conflicting code values and deterministic `next_steps` instead of raw strings.
  - Size admin CRUD should stay machine-readable too: missing sizes and duplicate size labels now return structured `size_not_found` / `size_label_already_exists` details with ids or conflicting labels and deterministic `next_steps` instead of raw strings.
  - Stock-balance admin CRUD should stay machine-readable too: missing stock-balance rows and duplicate `(sku_unit_id, warehouse_id)` pairs now return structured `stock_balance_not_found` / `stock_balance_pair_already_exists` details with ids/pair values and deterministic `next_steps` instead of raw strings.
  - SKU-unit admin CRUD should stay machine-readable too: missing SKU units and duplicate `(article_id, color_id, size_id)` combinations now return structured `sku_unit_not_found` / `sku_unit_combination_already_exists` details with ids/combination values and deterministic `next_steps` instead of raw strings.
  - Planning-settings admin CRUD should stay machine-readable too: missing planning-settings rows and duplicate `article_id` ownership now return structured `planning_settings_not_found` / `planning_settings_article_already_exists` details with ids/article ids and deterministic `next_steps` instead of raw strings.
  - Bundle-type admin CRUD should stay machine-readable too: missing bundle types and duplicate bundle-type codes now return structured `bundle_type_not_found` / `bundle_type_code_already_exists` details with ids/code values and deterministic `next_steps` instead of raw strings.
  - Color admin CRUD should stay machine-readable too: missing colors and duplicate `inner_code` values now return structured `color_not_found` / `color_inner_code_already_exists` details with ids/inner-code values and deterministic `next_steps` instead of raw strings.
  - Bundle-recipe admin CRUD should stay machine-readable too: missing bundle recipes and duplicate `(article_id, bundle_type_id, color_id, position)` combinations now return structured `bundle_recipe_not_found` / `bundle_recipe_combination_already_exists` details with ids/combination values and deterministic `next_steps` instead of raw strings.
  - Article admin CRUD should stay machine-readable too: missing articles and duplicate article codes now return structured `article_not_found` / `article_code_already_exists` details with ids/code values and deterministic `next_steps` instead of raw strings.
  - Operator-facing article lookup failures should stay aligned too: direct proposal, `from-wb` proposal, production-order settings, and WB readiness now return the same structured `404` `article_not_found` detail (`code/message/article_id/next_steps`) instead of raw strings.
  - `from-wb` readiness should stay aligned with the live proposal preflight contract too: `/api/v1/wb/from-wb/readiness` now honors request-level freshness threshold overrides with the same precedence/source tracing (`request -> admin_defaults -> global_default`) and preserves proposal-path partial-data acceptance (sales-only or stock-only can still be ready when freshness resolves to `fresh`).
  - Planning config snapshot should stay machine-readable on misses as well: missing article and missing article-level planning configuration now return structured `404` details (`article_not_found` / `no_planning_settings_found`) with `article_id` and deterministic `next_steps` instead of raw strings.
  - Read-only article endpoints should follow the same pattern: `article-dashboard` and `article-bundle-snapshot` now return structured `article_not_found` detail (`code/message/article_id/next_steps`) instead of raw strings.
  - Read-only bundle availability should follow the same operator contract discipline: missing article, bundle type, warehouse, and missing recipe on `bundle-availability` now return structured details (`article_not_found`, `bundle_type_not_found`, `warehouse_not_found`, `no_bundle_recipe`) instead of raw strings.
  - Read-only bundle deficit should match the same contract discipline: invalid `target_count`, missing article, bundle type, warehouse, and missing recipe on `bundle-deficit` now return structured details (`invalid_target_count`, `article_not_found`, `bundle_type_not_found`, `warehouse_not_found`, `no_bundle_recipe`) instead of raw strings.
  - Read-only order explanation should preserve the same machine-readable missing-article contract too: filtered `order-explanation-portfolio` requests now handle missing articles via structured `article_not_found` detail internally while keeping the existing skip-missing response semantics.
- WB onboarding/live-sync account resolution should stay machine-readable too: missing active account, unknown `account_id`, and empty `api_token` now return structured `400` details (`code/account_id?/next_steps`) instead of plain strings before any external WB call is attempted.
- WB transport/response failures should also stay machine-readable: request/network failures, rate limits, unauthorized tokens, invalid JSON, and invalid rows-format payloads now return structured WB error details with deterministic `code`, `next_steps`, and upstream metadata where available.
- WB object-response helpers should stay machine-readable as well: `_wb_get_json_object` now returns structured `wb_api_invalid_object_format` detail with deterministic `next_steps` instead of a raw string when an object endpoint responds with a non-object payload.
- WB replenishment request validation should stay machine-readable too: invalid date ordering (`wb_arrival_date < target_date`) on `POST /api/v1/wb/manager/proposal` now returns a structured validation detail with both dates, invalid field, and deterministic `next_steps`.
- WB shipment creation validation should stay aligned too: invalid date ordering (`wb_arrival_date < target_date`) on `POST /api/v1/wb/manager/shipment/from-proposal` now returns the same structured validation detail with both dates, invalid field, and deterministic `next_steps`.
- WB shipment resource misses should stay machine-readable too: read/edit shipment surfaces now return structured `wb_shipment_not_found` / `wb_shipment_item_not_found` details with resource IDs and deterministic `next_steps` instead of raw strings.
- WB shipment headers sorting should stay machine-readable too: invalid `sort_by` / `sort_dir` now return structured validation details, and the header-list path no longer crashes due to `status` query shadowing.
- WB shipment edit restrictions should stay machine-readable too: final shipment locks, non-draft item locks, and `final_qty > nsk_stock_available` now return structured validation details with shipment/item IDs and deterministic `next_steps` instead of raw strings.
- WB shipment status mutations should stay machine-readable too: unsupported status values and invalid status transitions now return structured validation details with shipment id, field context, allowed values/targets, and deterministic `next_steps` instead of raw strings.
- Planning monitoring timeseries validation should stay machine-readable too: missing `metrics` query parameter on `GET /api/v1/planning/monitoring/timeseries` now returns a structured `400` detail with field context and deterministic `next_steps` instead of a raw string.
- Planning monitoring alert-rule admin should stay machine-readable too: missing alert rules on `PATCH` / `DELETE /api/v1/planning/monitoring/alert-rules/{rule_id}` now return structured `alert_rule_not_found` details with `rule_id` and deterministic `next_steps` instead of a raw string.
- Purchase-order status mutations should stay machine-readable too: unsupported status values and invalid status transitions now return structured validation details with order id, field context, allowed values/targets, and deterministic `next_steps` instead of raw strings; cancelled orders continue to advertise self-transition semantics in that contract.
- Purchase-order read/item-edit guardrails should stay machine-readable too: missing purchase orders, missing purchase-order items, and non-draft item-edit locks now return structured details with order/item ids, status context where relevant, and deterministic `next_steps` instead of raw strings.
- Warehouse admin CRUD should stay machine-readable too: missing warehouses and duplicate warehouse codes now return structured `warehouse_not_found` / `warehouse_code_already_exists` details with ids or conflicting code values and deterministic `next_steps` instead of raw strings.
- Size admin CRUD should stay machine-readable too: missing sizes and duplicate size labels now return structured `size_not_found` / `size_label_already_exists` details with ids or conflicting labels and deterministic `next_steps` instead of raw strings.
- Stock-balance admin CRUD should stay machine-readable too: missing stock-balance rows and duplicate `(sku_unit_id, warehouse_id)` pairs now return structured `stock_balance_not_found` / `stock_balance_pair_already_exists` details with ids/pair values and deterministic `next_steps` instead of raw strings.
- SKU-unit admin CRUD should stay machine-readable too: missing SKU units and duplicate `(article_id, color_id, size_id)` combinations now return structured `sku_unit_not_found` / `sku_unit_combination_already_exists` details with ids/combination values and deterministic `next_steps` instead of raw strings.
- Planning-settings admin CRUD should stay machine-readable too: missing planning-settings rows and duplicate `article_id` ownership now return structured `planning_settings_not_found` / `planning_settings_article_already_exists` details with ids/article ids and deterministic `next_steps` instead of raw strings.
- Bundle-type admin CRUD should stay machine-readable too: missing bundle types and duplicate bundle-type codes now return structured `bundle_type_not_found` / `bundle_type_code_already_exists` details with ids/code values and deterministic `next_steps` instead of raw strings.
- Color admin CRUD should stay machine-readable too: missing colors and duplicate `inner_code` values now return structured `color_not_found` / `color_inner_code_already_exists` details with ids/inner-code values and deterministic `next_steps` instead of raw strings.
- Bundle-recipe admin CRUD should stay machine-readable too: missing bundle recipes and duplicate `(article_id, bundle_type_id, color_id, position)` combinations now return structured `bundle_recipe_not_found` / `bundle_recipe_combination_already_exists` details with ids/combination values and deterministic `next_steps` instead of raw strings.
- Article admin CRUD should stay machine-readable too: missing articles and duplicate article codes now return structured `article_not_found` / `article_code_already_exists` details with ids/code values and deterministic `next_steps` instead of raw strings.
- Operator-facing article lookup failures should stay aligned too: direct proposal, `from-wb` proposal, production-order settings, and WB readiness now return the same structured `404` `article_not_found` detail (`code/message/article_id/next_steps`) instead of raw strings.
- `from-wb` readiness should stay aligned with the live proposal preflight contract too: `/api/v1/wb/from-wb/readiness` now honors request-level freshness threshold overrides with the same precedence/source tracing (`request -> admin_defaults -> global_default`) and preserves proposal-path partial-data acceptance (sales-only or stock-only can still be ready when freshness resolves to `fresh`).
- Planning config snapshot should stay machine-readable on misses as well: missing article and missing article-level planning configuration now return structured `404` details (`article_not_found` / `no_planning_settings_found`) with `article_id` and deterministic `next_steps` instead of raw strings.
- Read-only article endpoints should follow the same pattern: `article-dashboard` and `article-bundle-snapshot` now return structured `article_not_found` detail (`code/message/article_id/next_steps`) instead of raw strings.
- Read-only bundle availability should follow the same operator contract discipline: missing article, bundle type, warehouse, and missing recipe on `bundle-availability` now return structured details (`article_not_found`, `bundle_type_not_found`, `warehouse_not_found`, `no_bundle_recipe`) instead of raw strings.
- Read-only bundle deficit should match the same contract discipline: invalid `target_count`, missing article, bundle type, warehouse, and missing recipe on `bundle-deficit` now return structured details (`invalid_target_count`, `article_not_found`, `bundle_type_not_found`, `warehouse_not_found`, `no_bundle_recipe`) instead of raw strings.
- Read-only order explanation should preserve the same machine-readable missing-article contract too: filtered `order-explanation-portfolio` requests now handle missing articles via structured `article_not_found` detail internally while keeping the existing skip-missing response semantics.

### 2.5 Layer 5 - Intervention signals (in progress)
- Deterministic unavoidable-stockout flags are wired from Layer 4 aggressive risk + risk-level context.
- Signal thresholds are explicit and deterministic (`accelerate_production` severe threshold, `increase_price_to_slow_velocity` unavoidable threshold).
- Layer 5 thresholds now support deterministic precedence (`request -> admin_defaults -> global_default -> code_default_constants`) with source tracing and safe threshold-order clamping.
- Layer 5 contract summary is now exposed (version/checks/status; threshold sanity, signal validity/order, reason-policy consistency) and projected in compact explainability mode.
- Layer 5 signal-only semantics are explicitly regression-locked: intervention signals may vary with thresholds while recommendation action remains unchanged for equivalent planning state.
- Current signal set: `accelerate_production` / `increase_price_to_slow_velocity` (dual signal allowed under severe in-flight risk).
- Keep signal-only behavior in v1 (no dynamic pricing model).

### 2.6 Explainability payload controls (in progress)
- Response explainability modes are now supported: `full` (default) and `compact` (summary + compact meta) for production-order direct/from-WB endpoints.
- Compact mode preserves deterministic planner outputs and trims explainability payload size (steps + heavy meta arrays).
- Compact-mode regressions now explicitly assert Layer 1-5 contract blocks are preserved for both direct and from-WB flows.
- Compact-mode regressions now also lock Layer 2 objective parameter/source tracing (`objective_parameters`, `objective_source`) for both direct and `from-wb` flows so economics evidence survives payload compaction.
- Compact/full parity harness now covers profile-based direct and from-WB scenarios (stockout / balanced / overstock style demand-stock setups) and validates Layer 2 decision-quality diagnostics plus Layer 2 contract decision-quality consistency checks are retained in compact metadata, while Layer 2 compact step text retains key readability fields (`decision_gate`, `reason_counts`, average profit gap, capital locked, contract status).
- Stabilize/lock compact-mode contract with additional regression coverage as Layer 1-5 semantics finalize.

## v1 Stable Alpha boundaries (strict)
- Deterministic, explainable, test-covered.
- No ML.
- No global optimization solver.
- No elasticity modeling expansion.
- No multi-warehouse logic.
- Do not turn planning core into a universal god-service for future domains.
- Do not bury shipment / receipt / discrepancy truth inside explainability-only payloads.
- Do not add finance or cash logic directly into recommendation orchestration.
- Do not assume merged warehouse scope is a permanent acceptable truth model.
- Prefer narrow helper/module boundaries that map to future real domains.
- Prefer event/state modeling for movement-related facts.

## Process constraints
- Feature branches only.
- Verify before merge.
- Design before implementation.
- Layer-by-layer delivery, no scope creep.
- Decision logging discipline is mandatory: every accepted architectural/product decision is documented immediately in the same work block.
- Documentation update is automatic after each accepted decision and must include at least `ROADMAP.md`, `STATUS.md`, and the relevant acceptance artifact (`PRODUCTION_ORDER_V1_STABLE_ALPHA_CHECKLIST.md` / casebook / ADR) to avoid omissions.

## Immediate high-leverage follow-ups
1. Finalize Layer 2 legacy-alias deprecation plan (`profit` / `expected_gross_profit` compatibility fields) after the deprecation window, without breaking stable API behavior during transition.
2. Lock regressions that prove allocation sensitivity to economics changes and preserve deterministic outputs.
3. Keep Layer 5 signal-only semantics explicit (no direct recommendation-action enforcement from intervention signals).
4. Keep explainability/contract expansion limited to behavior-critical checks while economics calibration evidence is finalized.
5. Freeze freshness/infrastructure work to bug-fix-only while economics release evidence is being locked.
6. Maintain explicit `Production Order v1 Stable Alpha` acceptance checklist (`PRODUCTION_ORDER_V1_STABLE_ALPHA_CHECKLIST.md`) as release gate.
7. Keep section 12 documentation-discipline checklist evidence current in each accepted work block while release candidate evidence is finalized.

## Phase 3 - Hardening and developer UX
- Docker-first test execution for environments without host `pytest`.
- PowerShell helper scripts for common workflows.
- Explicit OpenAPI stability notes for Planning Core endpoints.
- Clarify/document `backend2` purpose (lock-proof/e2e only).

## Phase 4 - Optional productization
- Auth and access control.
- Multi-tenant boundaries.
- Packaging/operational controls for external use.
