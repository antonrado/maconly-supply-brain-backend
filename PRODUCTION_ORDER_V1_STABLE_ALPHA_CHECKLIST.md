# Production Order v1 Stable Alpha - Acceptance Checklist

## 1) Scope and boundaries
- [x] Layered deterministic pipeline is active for production-order proposal (`Layer 1 -> 2 -> 3 -> 4 -> 5`).
- [x] Out-of-scope constraints are respected: no ML, no solver optimization, no multi-warehouse logic, no dynamic pricing model rollout.
- [x] Current-stage focus remains narrow and MVP-safe: supply planning engine, inventory truth, explainability/trust basics, from-WB parity, inbound/in-transit truth, receiving reconciliation, and operational visibility basics.
- [x] Architecture direction is kept explicit without scope expansion: planning core is not treated as a universal god-service, finance/cash logic is not embedded into recommendation orchestration, and shipment/receipt/discrepancy truth is not intentionally buried inside explainability-only payloads.
- [x] Current design still leaves room for future modular domains (`master_data`, `inventory_truth`, `supply_planning`, `inbound_tracking`, `receiving_reconciliation`, `wb_replenishment`, `leakage_control`, `sales_intelligence`, `finance`, `cash_planning`, `trust_and_exceptions`) even though those modules are not being implemented now.

## 2) API contract stability
- [x] `POST /api/v1/planning/core/production-order/proposal` returns stable structured response.
- [x] `POST /api/v1/planning/core/production-order/proposal/from-wb` returns stable structured response.
- [x] Request validation rejects malformed payloads with deterministic 4xx errors.
- [x] Unknown `article_id` is rejected deterministically with `404 Article not found` for both direct and from-WB endpoints.
- [x] Live connectivity smoke check passes via `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\dev.ps1 po-api-smoke` (health `200`, seeded direct/from-WB happy-path requests `200`, schema validations `422`).

## 3) Layer 1 contract (stock health)
- [x] Layer 1 emits per-SKU metrics in `explanation.meta.layer_1_stock_health.metrics`.
- [x] Layer 1 contract block exists in `explanation.meta.layer_1_stock_health.contract` with status `ok` for valid flows.
- [x] Contract checks include at least:
  - [x] unique color-size keys
  - [x] risk bounds validation
  - [x] non-negative quantity/velocity/coverage invariants
- [x] Assorti classification precedence is deterministic and traceable:
  - [x] `bundle_type.is_assorti`
  - [x] admin fallback mapping
  - [x] global fallback mapping
  - [x] missing/default main fallback

## 4) Layer 2 contract (allocation)
- [x] Allocation decision is composite-objective based (`objective_score_if_main_until_eta` vs `objective_score_if_assorti_until_eta`), not rule-based classification.
- [x] Canonical objective-score diagnostics are present in Layer 2 decisions/diagnostics during transition, while expected-gross-profit aliases remain available (`expected_gross_profit_*` fields and canonical/objective reason counts).
- [x] Decision gate default presentation is `composite_objective_until_eta` with explicit legacy alias `profit_until_eta`.
- [x] Layer 2 emits explicit legacy-alias deprecation-plan diagnostics in decision-quality/meta/alpha-proxy payloads (canonical replacement map + `deprecated_after`) without breaking legacy fields.
- [x] Tie-break is `hold`.
- [x] GMROI is diagnostic-only.
- [x] `explanation.meta.layer_2_allocation.summary` and `decisions` are present in full mode.
- [x] Layer 2 contract block exists in `explanation.meta.layer_2_allocation.contract` with status `ok` for valid flows.
- [x] Contract checks include at least:
  - [x] decision reason mapping matches allocation decision
  - [x] objective-score decision reason mapping matches allocation decision (`decision_reason_objective_score`)
  - [x] allocation decision matches composite objective gate outcome (`main|assorti|hold`) with legacy alias compatibility
  - [x] tie/near-tie flags match objective-gap math with legacy profit-gap alias compatibility
  - [x] profit-gap and GMROI-gap fields are internally consistent
  - [x] objective-score-gap field is internally consistent with objective score pair (`objective_score_gap_until_eta = |objective_score_if_main_until_eta - objective_score_if_assorti_until_eta|`)
  - [x] capital-locked metric is valid (non-negative numeric)
  - [x] objective-component formula is consistent (`objective_score = expected_gross_profit - capital_cost_penalty - stockout_penalty - overstock_penalty`)

## 5) Layer 3 contract (purchase shaping)
- [x] Layer 3 applies deterministic base factors by decision (`main|assorti|hold`).
- [x] Risk-weighted calibration is applied deterministically (stockout boost + overstock dampening + bounded factors).
- [x] Layer 3 shapes reorder quantities and does not replace Layer 2 allocation gate semantics.
- [x] Calibration override evidence confirms Layer 3 can change shaped qty/reorder units while Layer 2 allocation summary/decisions stay unchanged.
- [x] Layer 3 diagnostics are present in `explanation.meta.layer_3_purchase_shaping`:
  - [x] `qty_before`, `qty_after_base`, `qty_after`, `qty_delta_vs_base`
  - [x] calibration method, bounds, and factor summary

## 6) Layer 4 contract (scenarios)
- [x] Scenarios include `Conservative`, `Balanced`, `Aggressive`.
- [x] Scenario factor list is exposed in `explanation.meta.layer_4_scenarios.factors`.
- [x] Layer 4 contract summary exists in `explanation.meta.layer_4_scenarios.contract`.
- [x] Contract checks verify order and monotonic invariants.
- [x] Layer 4 contract checks also verify scenario delta integrity against `Balanced` baseline (`capital/revenue/profit/objective` delta consistency + `gross_profit_delta_vs_balanced` alias consistency).
- [x] Per-scenario money outputs are present:
  - [x] `expected_revenue`
  - [x] `expected_gross_profit`
  - [x] `expected_margin_percent`
  - [x] `expected_turnover_days`
  - [x] `stockout_probability_proxy`
  - [x] `overstock_risk_proxy`
- [x] Capital-gap transparency exists in `explanation.meta.capital_gap` (`available_capital`, `required_capital`, `deficit_or_surplus`) and is signal-only (no auto-policy override).

## 7) Layer 5 contract (interventions)
- [x] Layer 5 uses explicit threshold policy with traceable thresholds in `explanation.meta.layer_5_intervention.signal_thresholds`.
- [x] Policy output is deterministic and includes `signal_policy`, `reason`, and `signals`.
- [x] Severe risk + in-flight path may return dual signals as designed.
- [x] Layer 5 remains signal-only and does not directly enforce recommendation action.

## 8) Explainability contract
- [x] `explainability_mode` supports `full|compact` for direct and from-WB requests.
- [x] `full` preserves detailed steps and detailed meta arrays/maps.
- [x] `compact` preserves deterministic decisions while trimming payload-heavy explanation blocks.
- [x] `explanation.meta.explainability` is present in compact mode and reports omitted step count.
- [x] `compact` preserves contract blocks for Layers 1-5 and `alpha_proxy_economics`.
- [x] `compact` also preserves Layer 2 economics evidence on direct and `from-wb` flows: `objective_parameters`, `objective_source`, and mirrored `alpha_proxy_economics.layer_2_objective_parameters` remain available after compaction.

## 9) From-WB ingestion/freshness
- [x] Freshness mode (`warn|strict`) behavior is deterministic and covered.
- [x] Freshness threshold source precedence (`request > admin_defaults > global_default`) is traceable.
- [x] `/api/v1/wb/from-wb/readiness` honors request-level freshness threshold overrides with the same precedence/source tracing as the live `from-wb` proposal path.
- [x] `/api/v1/wb/from-wb/readiness` preserves proposal-path partial-data acceptance: sales-only or stock-only mapped articles can still be ready when freshness resolves to `fresh`.
- [x] Observed realized-price calibration from WB revenue window is traceable in `meta.from_wb.economic_observed_prices` and deterministic anomaly filtering (`max deviation=30%`) is covered.
- [x] `meta.from_wb` contains stable as-of trace and compact/full-consistent diagnostics.

## 10) Decision quality evidence (mandatory for stable alpha)
- [x] Casebook artifact is maintained in `PRODUCTION_ORDER_V1_DECISION_QUALITY_CASEBOOK.md`.
- [x] Provide 3 deterministic SKU case studies:
  - [x] Stockout risk case
  - [x] Balanced case
  - [x] Overstock case
- [x] For each case, include:
  - [x] Input metrics
  - [x] Intermediate layer outputs (`L1 -> L5`)
  - [x] Allocation decision reasoning
  - [x] Reorder quantity
  - [x] Scenario comparison (`Conservative|Balanced|Aggressive`)
  - [x] Capital impact proxy

## 10.1) Economic Alpha calibration (mandatory transition block)
- [x] Economics are formula-based and traceable (not proxy-only constants):
  - [x] `production_cost_per_unit`
  - [x] `logistics_cost_per_unit`
  - [x] `wb_commission_percent_main|assorti`
  - [x] `average_realized_price_main|assorti`
  - [x] `available_capital` (explicit capital governance input: default `strict` requires `request|admin_defaults|global_default`; opt-in `safe_default` applies zero-capital fallback with warning/meta tracing)
- [x] `explanation.meta.alpha_proxy_economics` contains economics source tracing (`economic_source`) and effective inputs (`economic_inputs`).
- [x] Trusted-economics diagnostics are emitted in full/compact explainability (`explanation.meta.economics_trust`, `explanation.meta.warnings`) and mirrored in `alpha_proxy_economics` (`economics_trust_level`, trust payload, warning block).
- [x] Source-tier precedence evidence is regression-locked for Economic Alpha inputs:
  - [x] `request`
  - [x] `admin_defaults`
  - [x] `global_default`
  - [x] `code_default_constants`
- [x] Layer 2 calibration coefficients are regression-locked for source-tier precedence on both direct and `from-wb` production-order paths: `layer2_capital_cost_rate`, `layer2_stockout_penalty_weight`, and `layer2_overstock_penalty_weight` resolve deterministically across `request -> admin_defaults -> global_default -> code_default_constants`, with effective values mirrored in `layer_2_allocation.objective_parameters` and `alpha_proxy_economics.layer_2_objective_parameters`.
- [x] Capital governance mode behavior is regression-locked for direct/from-WB flows: default `strict` unresolved `available_capital` returns deterministic `422` with actionable detail and explicit status `missing_available_capital_strict`, while opt-in `safe_default` applies zero-capital fallback with `HIGH` severity warning and `capital_governance` explainability tracing.
- [x] Layer 2 allocation is demonstrably sensitive to economics changes in regression tests.
- [x] Multi-regime end-to-end evidence is regression-locked for three distinct economics regimes (`stockout_dominates`, `overstock_dominates`, `commission_price_conflict`) across both direct and `from-wb` proposal paths, and the same release evidence survives compact mode, proving objective-vs-profit disagreement and expected Layer 5 intervention signals without adapter or compaction drift.
- [x] Layer 2 penalty-weight calibration evidence is documented with flip boundaries and frozen defaults, regressions assert emitted `objective_parameters` + `objective_source` in explainability payloads, and the documented stockout-profile / overstock-profile / commission-conflict boundaries are helper-regression locked (`stockout: 0.84 -> main`, `0.85 -> assorti`; `overstock: 0.02 -> main`, `0.03 -> assorti`; `commission conflict: 0.04 -> main`, `0.05 -> assorti`).
- [x] Capital-limited allocation is demonstrably objective-driven in regression tests (higher `objective_score_per_capital` may be selected before higher raw gross profit; no silent profit-only fallback).
- [x] Budget-limited proposal responses expose deterministic capital-constraint evidence in meta (`status=budget_limited_applied`, ranking, cutoff line incl. partial allocation when applicable).
- [x] For equal objective metrics, shared-capital ranking tie-break is deterministic and availability-priority aligned (`stockout_risk` higher first, then `overstock_risk` lower).
- [x] Capital-constraint meta includes runtime contract block (`version/checks/status`) and valid flows keep `capital_constraint.contract.status=ok` in full/compact responses, including ranking risk-priority consistency (`risk_priority_score = stockout_risk - overstock_risk`).
- [x] Phase 1 resource allocation core is regression-locked on the production-order path: per-article shared resources (`color x size`) are explicitly reserved across competing bundle strategies, no-double-use is enforced by runtime contract checks, and allocation consumption is exposed in both `constraints_applied.resource_allocation` and full/compact explainability metadata.
- [x] Phase 2 shared pantone pool is regression-locked on the production-order path: sibling WB article demand can reduce local fabric-min uplift for the same pantone, and shared-pool diagnostics are exposed in both `constraints_applied.fabric_min_batches` and full/compact explainability metadata.
- [x] Production-order API response now explicitly exposes `physical_scope` and `arrival_projection` contracts at the top level and mirrors both blocks in explainability metadata, including compact mode.
- [x] Arrival-horizon projection is regression-locked as a deterministic narrow slice (`ready now + competition-aware raw + effective in-flight @ arrival - demand until arrival`) and is wired into recommendation guardrail behavior (`safe_cover_until_arrival -> wait`, `shortage_before_arrival` remains shortage-driving).
- [x] Availability-first trust surfacing is regression-locked for capital-blocked shortages: when `arrival_projection.status=shortage_before_arrival` but capital constraint trims feasible quantity to zero and the facade remains `wait`, full/compact explainability emit a dedicated `HIGH` severity warning (`shortage_before_arrival_wait_blocked_by_capital_constraint`) instead of leaving that wait path unqualified.
- [x] Layer 5 threshold-order runtime corrections are regression-locked as explicit operator warnings: when `layer5_accelerate_production_risk_threshold` is below `layer5_unavoidable_stockout_risk_threshold` and is clamped upward at runtime, full/compact explainability emit a dedicated `MEDIUM` severity warning (`layer5_accelerate_threshold_clamped_to_unavoidable`) instead of relying only on proxy metadata (`layer5_threshold_order_adjusted`, `layer_proxy_source=...|clamped_to_unavoidable`).
- [x] Layer-proxy dirty-data runtime corrections are regression-locked as explicit operator warnings: when persisted Layer 3/5 proxy values fall outside the valid `[0,1]` interval and are ignored at runtime, full/compact explainability emit a dedicated `MEDIUM` severity warning (`layer_proxy_invalid_values_ignored_at_runtime`) instead of silently falling through to lower-precedence or code-default values.
- [x] Constraint-based supply-alignment helper ownership is facade-preserving and regression-locked: `planning_production_order_constraints.py` owns resource-allocation contract/build helpers and shared-pantone-pool snapshot logic, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
- [x] Capital constraint and Layer 4 helper ownership is facade-preserving and regression-locked: `planning_production_order_capital.py` owns objective-per-capital ranking, capital budget application, capital gap/contract summaries, and Layer 4 scenario/aggregate helpers, while `planning_production_order.py` preserves compatibility helper names, exported constants, and runtime explainability/response behavior.
- [x] Layer 1/2 decision helper ownership is facade-preserving and regression-locked: `planning_production_order_decision.py` owns stock-health metrics, Layer 1/2 contract summaries, Layer 2 allocation decisions, decision-quality diagnostics, and legacy-alias deprecation-plan helpers, while `planning_production_order.py` preserves compatibility helper names, exported constants, and runtime explainability/response behavior.
- [x] Layer 3 purchase-shaping helper ownership is facade-preserving and regression-locked: `planning_production_order_layer3.py` owns decision-based purchase factors, calibration bounds/weights, Layer 3 shaping, and Layer 3 contract summary, while `planning_production_order.py` preserves compatibility helper names, exported constants, and runtime explainability/response behavior.
- [x] Layer 5 intervention helper ownership is facade-preserving and regression-locked: `planning_production_order_layer5.py` owns intervention signal derivation, Layer 5 contract summary, and threshold-clamp operator warning, while `planning_production_order.py` preserves compatibility helper names, exported constants, and runtime explainability/response behavior.
- [x] From-WB adapter/calibration helper ownership is facade-preserving and regression-locked: `planning_production_order_from_wb.py` owns WB mapping detail builders, mapped-bundle resolution, WB stock/sales loaders, and observed price/commission calibration helpers, while `planning_production_order.py` preserves compatibility helper names, exported constants, monkeypatch-compatible `httpx` surface, and runtime explainability/response behavior.
- [x] Supply-policy helper ownership is facade-preserving and regression-locked: `planning_production_order_supply_policy.py` owns in-flight stage weighting, effective in-flight quantity estimation, and economic-buffer policy helpers, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
- [x] Settings/defaults helper ownership is facade-preserving and regression-locked: `planning_production_order_settings.py` owns effective settings resolution, admin size-weight loading, and admin in-flight defaults loading, while `planning_production_order.py` preserves compatibility helper names, the `_EffectiveSettings` surface, and runtime explainability/response behavior.
- [x] Elastic binding scope helper ownership is facade-preserving and regression-locked: `planning_production_order_elastic.py` owns active elastic-type applicability and scoped line-key resolution, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
- [x] Competition-aware raw stock helper ownership is facade-preserving and regression-locked: `planning_production_order_raw_stock.py` owns competition-aware raw bundle stock estimation, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
- [x] Capital helper facade cleanup is regression-locked: `planning_production_order_capital.py` solely owns `_bounded_unit_float` and `_compute_objective_components`, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
- [x] Allocation/uplift math helper ownership is facade-preserving and regression-locked: `planning_production_order_math.py` owns `_ceil_to_int`, `_normalize_weights`, `_allocate_units`, and `_add_units_for_color`, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
- [x] From-WB freshness helper ownership cleanup is facade-preserving and regression-locked: `planning_production_order_freshness.py` solely owns `_build_from_wb_freshness_snapshot` and `_resolve_from_wb_freshness_thresholds`, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
- [x] Layer 2 legacy-alias deprecation-plan facade cleanup is regression-locked: `planning_production_order_decision.py` solely owns `_build_layer2_legacy_alias_deprecation_plan`, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
- [x] Layer 1 stock-health helper ownership cleanup is regression-locked: `planning_production_order_decision.py` solely owns `_build_layer1_stock_health_metrics` and `_build_layer1_contract_summary`, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
 - [x] Layer 2 decision-helper ownership cleanup is regression-locked: `planning_production_order_decision.py` solely owns `_build_layer2_allocation_decisions`, `_build_layer2_contract_summary`, and `_build_layer2_decision_quality_summary`, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
 - [x] Layer 3 purchase-shaping ownership cleanup is regression-locked: `planning_production_order_layer3.py` solely owns `_apply_layer3_purchase_shaping` and `_build_layer3_contract_summary`, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
 - [x] Layer 4/capital ownership cleanup is regression-locked: `planning_production_order_capital.py` solely owns `_build_layer4_scenarios`, `_build_line_objective_capital_rankings`, `_apply_capital_constraint_to_candidate_lines`, `_build_capital_constraint_contract_summary`, `_build_capital_gap_summary`, `_build_layer4_contract_summary`, and `_build_layer4_aggregate_deltas`, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
 - [x] Layer 5 intervention ownership cleanup is regression-locked: `planning_production_order_layer5.py` solely owns `_build_layer5_intervention_signals` and `_build_layer5_contract_summary`, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
 - [x] Constraints constant ownership cleanup is regression-locked: `planning_production_order_constraints.py` solely owns `SHARED_COLOR_POOL_SOURCE`, `SHARED_COLOR_POOL_DEFAULT_OBSERVATION_WINDOW_DAYS`, and `RESOURCE_ALLOCATION_CONTRACT_VERSION`, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
 - [x] Elastic uplift ownership extraction is regression-locked: `planning_production_order_elastic.py` solely owns `_apply_elastic_min_batch_uplift`, including binding-scope resolution, elastic target selection, uplift allocation, and emitted elastic constraint payloads, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
  - [x] Shared color pool fabric uplift ownership extraction is regression-locked: `planning_production_order_constraints.py` solely owns `_apply_shared_color_pool_fabric_min_batches`, including shared pantone pool snapshot consumption, fabric min-batch uplift allocation, and emitted fabric constraint payloads, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
  - [x] Explainability warning aggregation ownership extraction is regression-locked: `planning_production_order_explainability.py` solely owns `_build_explanation_warnings`, including economics warning passthrough, invalid layer-proxy diagnostics, Layer 5 threshold clamp diagnostics, and shortage-wait/capital-blocked diagnostics, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
  - [x] Economics trust and capital governance ownership extraction is regression-locked: `planning_production_order_economics.py` solely owns `_resolve_economic_trust_and_capital_governance`, including economics-trust diagnostics, capital governance mode resolution, safe-default zero-capital fallback application, and strict missing-capital detail selection, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
  - [x] Alpha-proxy economics meta ownership extraction is regression-locked: `planning_production_order_explainability.py` solely owns `_build_alpha_proxy_economics_meta`, including economics source/input tracing, Layer 2/3/4/5 calibration diagnostics, and capital-governance/economics-trust surfacing in `alpha_proxy_economics`, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
  - [x] Explanation steps ownership extraction is regression-locked: `planning_production_order_explainability.py` solely owns `_build_explanation_steps`, including full explainability step-string packaging for demand/scope/source/layer/capital/elastic diagnostics, while `planning_production_order.py` preserves compatibility helper names, from-WB adapter prepend behavior, and runtime explainability/response behavior.
  - [x] Explanation meta ownership extraction is regression-locked: `planning_production_order_explainability.py` solely owns `_build_explanation_meta`, including full explainability meta packaging for source/scope/layer/capital/resource/elastic diagnostics, while `planning_production_order.py` preserves compatibility helper names, from-WB post-processing, and runtime explainability/response behavior.
  - [x] From-WB explainability ownership extraction is regression-locked: `planning_production_order_explainability.py` solely owns `_apply_from_wb_explainability`, including full `from_wb` meta packaging and `WB ingestion adapter` step prepend behavior, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
  - [x] From-WB runtime economic overrides ownership extraction is regression-locked: `planning_production_order_from_wb.py` solely owns `_build_from_wb_runtime_economic_overrides`, including observed price/commission normalization and runtime economics override plus per-field source-override packaging, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
  - [x] From-WB explainability input packaging ownership extraction is regression-locked: `planning_production_order_explainability.py` solely owns `_build_from_wb_explainability_inputs`, including as-of source resolution, sales-window derivation, bundle snapshot packaging, and text-formatted freshness fields for from-WB explainability application, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
  - [x] From-WB proposal request ownership extraction is regression-locked: `planning_production_order_from_wb.py` solely owns `_build_from_wb_proposal_request`, including delegated `ProductionOrderProposalRequest` assembly with bundle demand and bundle stock packaging for the from-WB flow, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
  - [x] From-WB strict freshness gate ownership extraction is regression-locked: `planning_production_order_freshness.py` solely owns `_raise_from_wb_strict_freshness_failure_if_needed`, including strict freshness rejection and structured `400` raising for stale/no-data from-WB inputs via injected detail-builder callback, while `planning_production_order.py` preserves compatibility helper names and runtime explainability/response behavior.
  - [x] From-WB preflight preparation ownership extraction is regression-locked: `planning_production_order_from_wb.py` solely owns `_build_from_wb_preflight_context`, including bundle mapping resolution, sales/stock snapshot loading, observed calibration loading, freshness threshold/snapshot preparation, strict freshness enforcement, delegated request assembly, and runtime override packaging for the from-WB flow, while `planning_production_order.py` preserves compatibility helper names plus façade compatibility surfaces (`httpx`, `EXPLAINABILITY_MODE_FULL`) and runtime explainability/response behavior.
  - [x] From-WB explainability finalization ownership extraction is regression-locked: `planning_production_order_explainability.py` solely owns `_finalize_from_wb_explainability`, including from-WB explainability input packaging, `from_wb` meta/step augmentation, and requested explainability-mode finalization for the from-WB response path, while `planning_production_order.py` preserves compatibility helper names plus façade compatibility surfaces (`httpx`, `EXPLAINABILITY_MODE_FULL`) and runtime explainability/response behavior.
  - [x] Typed from-WB preflight context ownership extraction is regression-locked: `planning_production_order_from_wb.py` solely owns `_FromWbPreflightContext` plus the typed return contract of `_build_from_wb_preflight_context`, replacing the loose dict payload with a frozen owner-side dataclass while `planning_production_order.py` preserves compatibility helper names plus façade compatibility surfaces (`httpx`, `EXPLAINABILITY_MODE_FULL`) and runtime explainability/response behavior.
  - [x] Full from-WB façade orchestration ownership extraction is regression-locked: `planning_production_order_from_wb.py` solely owns `_build_production_order_proposal_from_wb_response`, including article existence validation, preflight orchestration, delegated core proposal invocation, and response-side from-WB explainability finalization via injected callbacks, while `planning_production_order.py` preserves compatibility helper names plus façade compatibility surfaces (`httpx`, `EXPLAINABILITY_MODE_FULL`) and a thin public wrapper unchanged.
  - [x] From-WB mapping operator-contract ownership extraction is regression-locked: `planning_production_order_operator_contracts.py` solely owns `_build_from_wb_no_mapping_detail` and `_build_from_wb_missing_requested_bundle_type_detail`, while `planning_production_order_from_wb.py` consumes/re-exports them and `planning_production_order.py` preserves compatibility helper names plus façade compatibility surfaces (`httpx`, `EXPLAINABILITY_MODE_FULL`) unchanged.
  - [x] Shared article existence helper ownership extraction is regression-locked: `planning_production_order_article.py` solely owns `_build_article_not_found_detail` and `_require_article`, while `planning_production_order.py`, `planning_production_order_from_wb.py`, and `planning_production_order_admin.py` consume the shared helper and `planning_production_order_operator_contracts.py` re-exports the detail builder without changing façade compatibility surfaces (`httpx`, `EXPLAINABILITY_MODE_FULL`) or helper names.
  - [x] Admin settings operator-contract ownership extraction is regression-locked: `planning_production_order_operator_contracts.py` solely owns `_build_admin_settings_field_metadata` and `_build_admin_settings_validation_detail`, while `planning_production_order_admin.py` consumes/re-exports them and structured admin validation details remain unchanged.
  - [x] Production-order input preparation ownership extraction is regression-locked: `planning_production_order_inputs.py` solely owns `_PreparedProductionOrderInputs` and `_prepare_production_order_inputs`, including bundle recipe/SKU scope preparation, size-weight resolution, stock aggregation, in-flight effective supply normalization, WB bundle-stock fallback, and bundle demand/share packaging, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order line requirements ownership extraction is regression-locked: `planning_production_order_line_requirements.py` solely owns `_LineRequirementsPlan` and `_build_line_requirements_plan`, including deficit-to-line allocation preparation, color probability derivation, per-color size-weighted requirement allocation, and residual `line_qty` computation against current stock, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order constraint application ownership extraction is regression-locked: `planning_production_order_constraint_application.py` solely owns `_ConstraintApplicationResult` and `_apply_production_order_constraints`, including pantone/color override preparation plus fabric/shared-color and elastic constraint application orchestration, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order candidate line assembly ownership extraction is regression-locked: `planning_production_order_candidate_lines.py` solely owns `_build_candidate_lines`, including recommendation-line assembly from final `line_qty` plus per-line Layer 3/Layer 2 decision tagging in `source_reason`, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order capital application ownership extraction is regression-locked: `planning_production_order_capital_application.py` solely owns `_CapitalApplicationResult` and `_apply_production_order_capital_constraints`, including capital ranking orchestration, budget-constrained candidate-line application, capital contract packaging, and `candidate_total_units` derivation, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order Layer 4 application ownership extraction is regression-locked: `planning_production_order_layer4_application.py` solely owns `_Layer4ApplicationResult` and `_apply_production_order_layer4_analysis`, including expected-horizon-sales derivation plus Layer 4 scenario building, capital-gap packaging, contract summary packaging, and aggregate-delta packaging, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order Layer 5 application ownership extraction is regression-locked: `planning_production_order_layer5_application.py` solely owns `_Layer5ApplicationResult` and `_apply_production_order_layer5_analysis`, including Layer 5 intervention-signal orchestration, contract packaging, and `layer5_intervention_meta` packaging, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order scope/recommendation ownership extraction is regression-locked: `planning_production_order_scope_recommendation.py` solely owns `_ScopeRecommendationResult` and `_apply_production_order_scope_and_recommendation`, including physical-scope/arrival-projection orchestration plus recommendation/alternatives packaging, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order explanation-warnings ownership extraction is regression-locked: `planning_production_order_explanation_warning_application.py` solely owns `_ExplanationWarningApplicationResult` and `_apply_production_order_explanation_warnings`, including explanation-warning orchestration over runtime layer-proxy, arrival-projection, and capital-constraint inputs, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order alpha-proxy economics ownership extraction is regression-locked: `planning_production_order_alpha_proxy_application.py` solely owns `_AlphaProxyApplicationResult` and `_apply_production_order_alpha_proxy_economics`, including `layer4_scenario_factor_items` derivation plus alpha-proxy economics packaging for explainability/meta emission, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order explanation assembly ownership extraction is regression-locked: `planning_production_order_explanation_application.py` solely owns `_ExplanationApplicationResult` and `_apply_production_order_explanation`, including `ProductionOrderExplanationBlock` assembly, summary construction, and `_build_explanation_steps`/`_build_explanation_meta` orchestration, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order explainability-mode application ownership extraction is regression-locked: `planning_production_order_explainability_mode_application.py` solely owns `_ExplainabilityModeApplicationResult` and `_apply_production_order_explainability_mode`, including final explainability-mode application over `ProductionOrderExplanationBlock`, while `planning_production_order.py` preserves compatibility helper names, skipped-path behavior, and runtime response behavior.
  - [x] Production-order response assembly ownership extraction is regression-locked: `planning_production_order_response_application.py` solely owns `_ResponseApplicationResult` and `_apply_production_order_response`, including final `ProductionOrderProposalResponse` assembly for both `skipped` and `ok` production-order paths, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order skipped-path ownership extraction is regression-locked: `planning_production_order_skip_application.py` solely owns `_SkipApplicationResult` and `_apply_production_order_skip`, including the `include_in_planning=false` short-circuit path, skipped explanation construction, explainability-mode application, and skipped response assembly orchestration, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order economic-governance application ownership extraction is regression-locked: `planning_production_order_economic_governance_application.py` solely owns `_EconomicGovernanceApplicationResult` and `_apply_production_order_economic_governance`, including economic-trust/capital-governance application orchestration and strict missing-capital `422` short-circuit handling, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order settings-resolution application ownership extraction is regression-locked: `planning_production_order_settings_resolution_application.py` solely owns `_SettingsResolutionApplicationResult` and `_apply_production_order_settings_resolution`, including pre-pipeline effective settings, layer-proxy settings, and economic-settings resolution orchestration, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order settings-loading application ownership extraction is regression-locked: `planning_production_order_settings_loading_application.py` solely owns `_SettingsLoadingApplicationResult` and `_apply_production_order_settings_loading`, including article/planning/global settings DB loading orchestration for the direct production-order path, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order resource-allocation application ownership extraction is regression-locked: `planning_production_order_resource_allocation_application.py` solely owns `_ResourceAllocationApplicationResult` and `_apply_production_order_resource_allocation`, including resource-allocation orchestration plus `competition_raw_*` and `available_bundles_for_cover` packaging, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order assorti-classification application ownership extraction is regression-locked: `planning_production_order_assorti_application.py` solely owns `_AssortiApplicationResult` and `_apply_production_order_assorti_classification`, including admin/global assorti mapping parse, bundle-type assorti classification loading, assorti/main counts, and classification-source breakdown packaging, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order horizon application ownership extraction is regression-locked: `planning_production_order_horizon_application.py` solely owns `_HorizonApplicationResult` and `_apply_production_order_horizon`, including economic-buffer calculation plus target-horizon, required-bundles, and bundle-deficit packaging, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order risk application ownership extraction is regression-locked: `planning_production_order_risk_application.py` solely owns `_RiskApplicationResult` and `_apply_production_order_risk_level`, including `days_of_cover_estimate` and `risk_level` derivation for the direct production-order path, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order layer1-summary application ownership extraction is regression-locked: `planning_production_order_layer1_summary_application.py` solely owns `_Layer1SummaryApplicationResult` and `_apply_production_order_layer1_summary`, including average coverage derivation, high-stockout-risk counting, and Layer 1 contract packaging, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order layer2-summary application ownership extraction is regression-locked: `planning_production_order_layer2_summary_application.py` solely owns `_Layer2SummaryApplicationResult` and `_apply_production_order_layer2_summary`, including Layer 2 contract packaging and decision-quality packaging after allocation-decision derivation, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order layer2-allocation application ownership extraction is regression-locked: `planning_production_order_layer2_allocation_application.py` solely owns `_Layer2AllocationApplicationResult` and `_apply_production_order_layer2_allocation`, including Layer 2 allocation-decision derivation and summary packaging, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order line-requirements application ownership extraction is regression-locked: `planning_production_order_line_requirements_application.py` solely owns `_LineRequirementsApplicationResult` and `_apply_production_order_line_requirements`, including line-requirements plan orchestration plus `color_probability`, `line_required`, and `line_qty` packaging, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order layer3 application ownership extraction is regression-locked: `planning_production_order_layer3_application.py` solely owns `_Layer3ApplicationResult` and `_apply_production_order_layer3`, including Layer 3 purchase-shaping orchestration plus `layer3_decision_by_line`, `layer3_purchase_shaping`, and `layer3_contract` packaging, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order candidate-lines application ownership extraction is regression-locked: `planning_production_order_candidate_lines_application.py` solely owns `_CandidateLinesApplicationResult` and `_apply_production_order_candidate_lines`, including candidate-line assembly orchestration for the direct production-order path, while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order capital-unpack application ownership extraction is regression-locked: `planning_production_order_capital_unpack_application.py` solely owns `_CapitalUnpackApplicationResult` and `_apply_production_order_capital_unpack`, including post-capital result projection for the direct production-order path (`candidate_lines`, `capital_rankings`, `capital_constraint_summary`, `capital_constraint_contract`, `candidate_total_units`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order layer4-unpack application ownership extraction is regression-locked: `planning_production_order_layer4_unpack_application.py` solely owns `_Layer4UnpackApplicationResult` and `_apply_production_order_layer4_unpack`, including post-Layer-4 result projection for the direct production-order path (`expected_horizon_sales`, `layer4_scenarios`, `capital_gap_summary`, `layer4_contract`, `layer4_aggregate_deltas`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order layer5-unpack application ownership extraction is regression-locked: `planning_production_order_layer5_unpack_application.py` solely owns `_Layer5UnpackApplicationResult` and `_apply_production_order_layer5_unpack`, including post-Layer-5 result projection for the direct production-order path (`layer5_intervention`, `layer5_contract`, `layer5_intervention_meta`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order scope-recommendation-unpack application ownership extraction is regression-locked: `planning_production_order_scope_recommendation_unpack_application.py` solely owns `_ScopeRecommendationUnpackApplicationResult` and `_apply_production_order_scope_recommendation_unpack`, including post-scope-recommendation result projection for the direct production-order path (`physical_scope`, `arrival_projection`, `action`, `recommendation`, `alternatives`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order explanation-warning-unpack application ownership extraction is regression-locked: `planning_production_order_explanation_warning_unpack_application.py` solely owns `_ExplanationWarningUnpackApplicationResult` and `_apply_production_order_explanation_warning_unpack`, including post-explanation-warning result projection for the direct production-order path (`explanation_warnings`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order alpha-proxy-unpack application ownership extraction is regression-locked: `planning_production_order_alpha_proxy_unpack_application.py` solely owns `_AlphaProxyUnpackApplicationResult` and `_apply_production_order_alpha_proxy_unpack`, including post-alpha-proxy result projection for the direct production-order path (`layer4_scenario_factor_items`, `alpha_proxy_economics`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order explanation-unpack application ownership extraction is regression-locked: `planning_production_order_explanation_unpack_application.py` solely owns `_ExplanationUnpackApplicationResult` and `_apply_production_order_explanation_unpack`, including post-explanation result projection for the direct production-order path (`explanation`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order explainability-mode-unpack application ownership extraction is regression-locked: `planning_production_order_explainability_mode_unpack_application.py` solely owns `_ExplainabilityModeUnpackApplicationResult` and `_apply_production_order_explainability_mode_unpack`, including post-explainability-mode result projection for the direct production-order path (`explanation`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order response-unpack application ownership extraction is regression-locked: `planning_production_order_response_unpack_application.py` solely owns `_ResponseUnpackApplicationResult` and `_apply_production_order_response_unpack`, including post-response result projection for the direct production-order path (`response`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order resource-allocation-unpack application ownership extraction is regression-locked: `planning_production_order_resource_allocation_unpack_application.py` solely owns `_ResourceAllocationUnpackApplicationResult` and `_apply_production_order_resource_allocation_unpack`, including post-resource-allocation result projection for the direct production-order path (`resource_allocation`, `competition_raw_by_bundle`, `competition_raw_bundle_stock`, `competition_raw_breakdown`, `available_bundles_for_cover`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order assorti-unpack application ownership extraction is regression-locked: `planning_production_order_assorti_unpack_application.py` solely owns `_AssortiUnpackApplicationResult` and `_apply_production_order_assorti_unpack`, including post-assorti result projection for the direct production-order path (`admin_assorti_bundle_type_ids`, `global_assorti_bundle_type_ids`, `assorti_by_bundle_type`, `assorti_classification_by_bundle_type`, `assorti_bundle_type_count`, `main_bundle_type_count`, `assorti_classification_source_breakdown`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order layer2-allocation-unpack application ownership extraction is regression-locked: `planning_production_order_layer2_allocation_unpack_application.py` solely owns `_Layer2AllocationUnpackApplicationResult` and `_apply_production_order_layer2_allocation_unpack`, including post-layer2-allocation result projection for the direct production-order path (`layer2_allocation_decisions`, `layer2_allocation_summary`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order layer2-summary-unpack application ownership extraction is regression-locked: `planning_production_order_layer2_summary_unpack_application.py` solely owns `_Layer2SummaryUnpackApplicationResult` and `_apply_production_order_layer2_summary_unpack`, including post-layer2-summary result projection for the direct production-order path (`layer2_contract`, `layer2_decision_quality`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order layer1-summary-unpack application ownership extraction is regression-locked: `planning_production_order_layer1_summary_unpack_application.py` solely owns `_Layer1SummaryUnpackApplicationResult` and `_apply_production_order_layer1_summary_unpack`, including post-layer1-summary result projection for the direct production-order path (`layer1_avg_coverage_days`, `layer1_high_stockout_risk_count`, `layer1_contract`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order risk-unpack application ownership extraction is regression-locked: `planning_production_order_risk_unpack_application.py` solely owns `_RiskUnpackApplicationResult` and `_apply_production_order_risk_unpack`, including post-risk result projection for the direct production-order path (`days_of_cover_estimate`, `risk_level`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order horizon-unpack application ownership extraction is regression-locked: `planning_production_order_horizon_unpack_application.py` solely owns `_HorizonUnpackApplicationResult` and `_apply_production_order_horizon_unpack`, including post-horizon result projection for the direct production-order path (`economic_buffer_days`, `target_bundle_horizon_days`, `required_bundle_units`, `bundle_deficit_total`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order line-requirements-unpack application ownership extraction is regression-locked: `planning_production_order_line_requirements_unpack_application.py` solely owns `_LineRequirementsUnpackApplicationResult` and `_apply_production_order_line_requirements_unpack`, including post-line-requirements result projection for the direct production-order path (`color_probability`, `line_required`, `line_qty`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order layer3-unpack application ownership extraction is regression-locked: `planning_production_order_layer3_unpack_application.py` solely owns `_Layer3UnpackApplicationResult` and `_apply_production_order_layer3_unpack`, including post-layer3 result projection for the direct production-order path (`layer3_decision_by_line`, `layer3_purchase_shaping`, `layer3_contract`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order constraint-unpack application ownership extraction is regression-locked: `planning_production_order_constraint_unpack_application.py` solely owns `_ConstraintUnpackApplicationResult` and `_apply_production_order_constraint_unpack`, including post-constraint result projection for the direct production-order path (`constraints_applied`, `shared_color_pool`, `applicable_elastic_type_ids`, `elastic_scope_line_keys`, `elastic_scope_mode`, `scoped_elastic_rows_count`, `elastic_uplift_delta`, `elastic_uplift_scope`, `elastic_uplift_keys`, `elastic_uplift_line_alloc`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order candidate-lines-unpack application ownership extraction is regression-locked: `planning_production_order_candidate_lines_unpack_application.py` solely owns `_CandidateLinesUnpackApplicationResult` and `_apply_production_order_candidate_lines_unpack`, including post-candidate-lines result projection for the direct production-order path (`candidate_lines`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Production-order settings-loading-unpack application ownership extraction is regression-locked: `planning_production_order_settings_loading_unpack_application.py` solely owns `_SettingsLoadingUnpackApplicationResult` and `_apply_production_order_settings_loading_unpack`, including post-settings-loading result projection for the direct production-order path (`article_settings`, `planning_settings`, `global_settings`), while `planning_production_order.py` preserves compatibility helper names and runtime response behavior.
  - [x] Legacy planning endpoints are explicitly marked low-fidelity/deprecated without new planning logic: `/api/v1/planning/core/proposal` and `/api/v1/planning/order-proposal` emit successor/fidelity headers instead of silently pretending parity with production-order core.
  - [x] Direct production-order prerequisite failures are regression-locked as machine-readable operator contracts: missing `bundle_recipe` coverage and missing SKU scope for recipe colors return structured `400` details with deterministic `code`, affected IDs, and `next_steps`.
  - [x] Production-order settings admin validation failures are regression-locked as machine-readable operator contracts: invalid size ids, elastic binding scope mismatches, assorti bundle type ids, and in-flight color/size scope errors return structured `400` details with deterministic `code`, `field`, affected IDs, and `next_steps`.
- [x] WB integration account resolution failures are regression-locked as machine-readable operator contracts: missing active account, unknown `account_id`, and empty `api_token` return structured `400` details with deterministic `code`, optional `account_id`, and `next_steps` before live sync requests proceed.
- [x] WB transport/response helper failures are regression-locked as machine-readable operator contracts: request/network failures, `429` rate limits, `401` token failures, invalid JSON, and invalid rows-format payloads return structured details with deterministic `code`, `next_steps`, and upstream metadata where available.
- [x] WB object-response helper failures are regression-locked as machine-readable operator contracts: `_wb_get_json_object` returns structured `wb_api_invalid_object_format` detail with deterministic `next_steps` when an object endpoint responds with a non-object payload.
- [x] WB replenishment request validation is regression-locked as a machine-readable operator contract: invalid `wb_arrival_date < target_date` now returns a structured detail with both dates, invalid field, and deterministic `next_steps`.
- [x] WB shipment creation validation is regression-locked as a machine-readable operator contract: invalid `wb_arrival_date < target_date` on `shipment/from-proposal` now returns the same structured detail with both dates, invalid field, and deterministic `next_steps`.
- [x] WB shipment resource misses are regression-locked as machine-readable operator contracts: missing shipments and shipment items now return structured `wb_shipment_not_found` / `wb_shipment_item_not_found` details with resource IDs and deterministic `next_steps`.
- [x] WB shipment header sort validation is regression-locked as a machine-readable operator contract: invalid `sort_by` / `sort_dir` now return structured details, and the `status` query shadowing bug on the header-list path is fixed.
- [x] WB shipment edit restrictions are regression-locked as machine-readable operator contracts: final shipment locks, non-draft item locks, and `final_qty > nsk_stock_available` now return structured details with shipment/item IDs and deterministic `next_steps`.
- [x] WB shipment status mutations are regression-locked as machine-readable operator contracts: unsupported status values and invalid status transitions now return structured details with shipment id, field context, allowed values/targets, and deterministic `next_steps`.
- [x] Planning monitoring timeseries validation is regression-locked as a machine-readable operator contract: missing `metrics` query parameter now returns a structured `400` detail with field context and deterministic `next_steps`.
- [x] Planning monitoring alert-rule admin is regression-locked on machine-readable `404` details: missing alert rules now return structured `alert_rule_not_found` payloads with `rule_id` and deterministic `next_steps` on update/delete paths.
- [x] Purchase-order status mutations are regression-locked as machine-readable operator contracts: unsupported status values and invalid status transitions now return structured details with order id, field context, allowed values/targets, and deterministic `next_steps`; cancelled orders preserve self-transition semantics in the emitted contract.
- [x] Purchase-order read/item-edit guardrails are regression-locked as machine-readable operator contracts: missing orders, missing purchase-order items, and non-draft item-edit locks now return structured details with order/item ids, status context where relevant, and deterministic `next_steps`.
- [x] Warehouse admin CRUD is regression-locked on machine-readable lookup/conflict contracts: missing warehouses and duplicate warehouse codes now return structured `warehouse_not_found` / `warehouse_code_already_exists` details with ids or conflicting code values and deterministic `next_steps`.
- [x] Size admin CRUD is regression-locked on machine-readable lookup/conflict contracts: missing sizes and duplicate size labels now return structured `size_not_found` / `size_label_already_exists` details with ids or conflicting labels and deterministic `next_steps`.
- [x] Stock-balance admin CRUD is regression-locked on machine-readable lookup/conflict contracts: missing stock-balance rows and duplicate `(sku_unit_id, warehouse_id)` pairs now return structured `stock_balance_not_found` / `stock_balance_pair_already_exists` details with ids/pair values and deterministic `next_steps`.
- [x] SKU-unit admin CRUD is regression-locked on machine-readable lookup/conflict contracts: missing SKU units and duplicate `(article_id, color_id, size_id)` combinations now return structured `sku_unit_not_found` / `sku_unit_combination_already_exists` details with ids/combination values and deterministic `next_steps`.
- [x] Planning-settings admin CRUD is regression-locked on machine-readable lookup/conflict contracts: missing planning-settings rows and duplicate `article_id` ownership now return structured `planning_settings_not_found` / `planning_settings_article_already_exists` details with ids/article ids and deterministic `next_steps`.
- [x] Bundle-type admin CRUD is regression-locked on machine-readable lookup/conflict contracts: missing bundle types and duplicate bundle-type codes now return structured `bundle_type_not_found` / `bundle_type_code_already_exists` details with ids/code values and deterministic `next_steps`.
- [x] Color admin CRUD is regression-locked on machine-readable lookup/conflict contracts: missing colors and duplicate `inner_code` values now return structured `color_not_found` / `color_inner_code_already_exists` details with ids/inner-code values and deterministic `next_steps`.
- [x] Bundle-recipe admin CRUD is regression-locked on machine-readable lookup/conflict contracts: missing bundle recipes and duplicate `(article_id, bundle_type_id, color_id, position)` combinations now return structured `bundle_recipe_not_found` / `bundle_recipe_combination_already_exists` details with ids/combination values and deterministic `next_steps`.
- [x] Article admin CRUD is regression-locked on machine-readable lookup/conflict contracts: missing articles and duplicate article codes now return structured `article_not_found` / `article_code_already_exists` details with ids/code values and deterministic `next_steps`.
- [x] Operator-facing `404` article lookup failures are regression-locked as a shared machine-readable contract across direct proposal, `from-wb` proposal, production-order settings, and WB readiness surfaces (`code=article_not_found`, `article_id`, `next_steps`).
- [x] Planning config snapshot miss-cases are regression-locked as machine-readable operator contracts: missing article returns `article_not_found`, and missing article-level planning config returns `no_planning_settings_found`, both with `article_id` and deterministic `next_steps`.
- [x] Read-only article endpoints `article-dashboard` and `article-bundle-snapshot` are regression-locked on the same machine-readable `article_not_found` contract (`article_id`, `next_steps`) instead of raw strings.
- [x] Read-only `bundle-availability` lookup/prerequisite failures are regression-locked as machine-readable operator contracts: missing article, bundle type, warehouse, and missing recipe now return deterministic structured details instead of raw strings.
- [x] Read-only `bundle-deficit` validation/lookup/prerequisite failures are regression-locked as machine-readable operator contracts: invalid `target_count`, missing article, bundle type, warehouse, and missing recipe now return deterministic structured details instead of raw strings.
- [x] Read-only `order-explanation-portfolio` preserves skip-missing semantics while using the same machine-readable `article_not_found` contract internally for filtered missing article ids.
- [x] Scope guard preserved while implementing economics: no ML, no solver, no multi-warehouse, no non-economics feature expansion.
- [x] Any R5 modularization remains narrow and facade-preserving: extracted helpers may move to dedicated service modules (currently economics + compact explainability + assorti classification + layer-proxy settings), but `planning_production_order.py` keeps the external service surface/compatibility imports unchanged and regressions remain green.

## 11) Verification gate
- [x] Run verification suite and confirm green:
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\dev.ps1 verify`
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\dev.ps1 verify-live` (targeted local gate: verify + production-order live API smoke checks)
- [x] Regression tests include Layer 1-5 contracts + explainability compact/full coverage.
- [x] Decision quality case studies are documented and reviewable for external sanity check.
- [x] Current physical-scope / arrival-projection / legacy-marker block is revalidated by `tests/test_planning_core_production_order_api.py`, `tests/test_end_to_end_planning.py`, and `scripts/dev.ps1 verify`.
- [x] Working tree is clean after final commit.

## 12) Decision documentation discipline (mandatory)
- [x] Each accepted architectural/product decision is documented in the same work block (no deferred doc updates).
- [x] `ROADMAP.md` is updated to reflect plan-level impact of the accepted decision.
- [x] `STATUS.md` is updated to reflect implementation/runtime state of the accepted decision.
- [x] Relevant acceptance artifact is updated for the accepted decision (`PRODUCTION_ORDER_V1_STABLE_ALPHA_CHECKLIST.md` / casebook / ADR).
- [x] Documentation update behavior is explicit: after each accepted decision, documentation updates are applied automatically to prevent omissions.
